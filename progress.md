# Progress Log — 월급 배분 관리 서비스

최신 항목이 위. 한 세션 = 한 항목. 다음 세션(새 컨텍스트)이 이걸 읽고 이어받는다.
작업 전 읽고, 멈추기 전 추가할 것.

<!-- 세션마다 이 템플릿 복사:

## YYYY-MM-DD  —  [요구사항ID] 작업명
- 한 일:      이번 세션에 구현/변경한 것
- 초록불:     ./gradlew verify / npm run verify 통과 여부, 통과한 골든 케이스
- passes 전환: true로 바뀐 feature_list id
- 다음:       다음 최우선 미완료 항목
- 메모:       추가한 Flyway 버전, 새 상수, 결정사항, 주의점
-->

## 2026-06-14  —  [SCR-03b 완료] 홈 월급날 체크리스트 카드 — 실수령액 확인 + 통장별 이체 체크/건너뛰기
- 한 일: Phase 2 백엔드 체인(CYCLE-03 스냅샷·CYCLE-04 실수령액·CYCLE-06 체크리스트 조회/전이)이 전부 닫힌 위에 프론트 동선을 얹어 사용자에게 보이게 만듦(화면흐름도 SCR-03 "지급일~D+3 체크리스트 카드"). ① `api/cycle.ts`(신규) — `getCurrentCycle`(GET /cycles/current)·`confirmIncome`(PATCH /cycles/{id}/income)·`changeLineStatus`(PATCH /plan-lines/{id}) + 타입(CurrentCycle/ChecklistAccountGroup/ChecklistLine/PlanLineStatus). **런타임 const export 없음**(types+함수만) → auto-mock 안전(메모리 `vimock-breaks-const-exports` 회피). ② `components/ChecklistCard.vue`(신규) — 마운트 시 자체 조회 후 **KST 오늘이 cycleStart~D+3 구간일 때만** 노출(구간 밖·404 NOT_FOUND이면 조용히 숨김, 폭포 표시 안 막음). 실수령액 확인(BottomSheet 수정 → confirmIncome → 체크리스트 재조회로 LIVING 재계산 반영), 통장별 라인 체크(PENDING↔DONE)·건너뛰기(→SKIPPED)·되돌리기(→PENDING), **로컬 진행도 재계산**(done=PENDING 아닌 라인 수, 서버 progress 의미론과 동일). LIVING 머신 토큰은 `home.living` i18n 렌더(규칙 7). ③ `views/HomeView.vue` — 카드를 홈 최상단에 얹음(폭포와 독립, 카드가 스스로 노출 판정). ④ `locales/ko.json`·`en.json` — `checklist.*` 키 추가(ko/en 동기). 항목 편집(ITEM-07)은 여전히 백엔드 미구현이라 v1 제외.
- 초록불: `npm run verify` 통과 — **68 tests**(기존 62 + 신규 6: `ChecklistCard.spec.ts` — 지급일 당일 노출[실수령액·통장별 4라인·진행도 1/4·그룹 3], D+5 구간 밖 숨김, 404 조용히 숨김, 라인 체크→DONE 전이+진행도 2/4, 건너뛰기→SKIPPED, 실수령액 수정→confirmIncome 호출+재조회). eslint+vue-tsc+vitest+build 전부 초록, `HomeView.spec`은 cycle API auto-mock 추가로 무손상. `./gradlew verify` BUILD SUCCESSFUL(백엔드 무변경, UP-TO-DATE). 골든 fixture 무수정.
- passes 전환: **SCR-03b → true**.
- 다음: 위임 가능한 미완료는 **ITEM-07**(항목 수정 적용 시점 — 구현규칙 4장 재생성: 다음 사이클 기본, '이번 달 반영' 시에만 현재 사이클 미완료 plan_line 재계산·완료 라인 보존. CYCLE-06 상태 전이 위에 얹힘, budget_items PATCH `?applyToCurrentCycle=true`). 그 외 미완료는 수동 **VERIFY-notion-match**(실 OAuth/브라우저 — 이번 카드의 라이브 노출 확인이 여기 포함). 실 운용엔 일일 판정 @Scheduled→createSnapshot 배선(CYCLE-03 후속)도 여전히 필요.
- 메모: ① **노출 구간 판정은 프론트(KST)** — 백엔드 `GET /cycles/current`는 "오늘 속한 사이클"(cycle_start≤today≤cycle_end)을 주지만, 카드 등장은 지급일~D+3로 더 좁다. 그래서 카드가 `Intl.DateTimeFormat('en-CA', {timeZone:'Asia/Seoul'})`로 KST 오늘을 구해 `dayDiff(cycleStart, today)∈[0,3]`을 판정. 일수 계산은 `Date.UTC` 자정 기준이라 로컬 TZ·DST 무관(규칙 3 정신을 프론트에서 지킴). ② **실수령액 수정 후 전체 재조회(결정)** — `PATCH /cycles/{id}/income` 응답(CycleResponse)엔 라인이 없고 LIVING이 서버에서 재계산되므로, 확인 성공 후 `getCurrentCycle`를 다시 불러 체크리스트를 갱신(낙관적 부분 갱신 대신 정합성 우선). ③ **진행도는 로컬 재계산** — 토글 즉시 반영 위해 서버 `progress` 필드 대신 라인 상태로 `done=처리됨(PENDING 아님)/total`을 계산. 백엔드 CYCLE-06 의미론과 동일(건너뜀도 done 집계). ④ **404=숨김, 기타 오류도 숨김** — 카드는 홈 보조 요소라 스냅샷 미생성(404)·일시 오류 시 폭포 표시를 막지 않게 cycle=null로 비노출(404가 아닌 오류만 errorCode 기록, 단 비노출이라 표면화 안 됨). ⑤ **LIVING i18n(규칙 7)** — line.name이 머신 토큰 'LIVING'이면 `home.living`로, 항목 라인은 스냅샷 이름 그대로. 백엔드가 name_snapshot에 'LIVING' 저장(CYCLE-03 메모 ①)한 계약을 프론트가 매핑. ⑥ **통장 그룹 렌더(결정)** — 백엔드가 account_id로 group by한 구조를 그대로 살려 그룹 헤더(통장명+합계) + 라인별 체크 행으로 렌더(체크 단위는 plan_line). 목업의 통장당 1줄은 단일 라인 케이스의 특수형. ⑦ **lint 함정 2건** — vitest 플러그인 `require-mock-type-parameters`(factory `vi.fn()`에 타입 인자 강제) → HomeView는 factory 대신 `vi.mock('@/api/cycle')` auto-mock으로 회피(undefined 반환→카드 숨김). `noUncheckedIndexedAccess` → 테스트 `findAll()[0]`·날짜 split은 비널 단언(`!`, 기존 spec 전례)·`utcMidnight` 헬퍼로 해소. ⑧ **Flyway·골든 변화 없음**(프론트 전용). spotless 무관(프론트는 oxlint+eslint, prettier 없음 — 메모리 `ps1-hooks-need-utf8-bom`). ⑨ 커밋 6efec63(feat) + 본 doc.

## 2026-06-14  —  [CYCLE-06 완료] 통장별 체크리스트 조회 + 라인 상태 전이
- 한 일: CYCLE-03(스냅샷 영속화)·CYCLE-04(실수령액 확인) 위에 체크리스트 **조회·상태 전이**를 얹음(API명세 5장). owner 도메인(폭포·스냅샷 생성)은 손대지 않고 plan_lines를 통장별로 묶고 상태만 전이하는 위임 범위. ① `cycle/CycleController` **`GET /api/v1/cycles/current`**(인증 필수) — 오늘 사이클 헤더 + 통장별 체크리스트 + 진행도. ② `cycle/PlanLineController`(신규, `/api/v1/plan-lines`) **`PATCH /{id}`** — body `{status:"PENDING"|"DONE"|"SKIPPED"}`(잘못된 enum은 역직렬화에서 VALIDATION_FAILED), 갱신된 라인 단건 반환. ③ `cycle/CycleChecklistService`(@Service, application) — `getCurrentChecklist`(readOnly, 오늘이 속한 사이클 `cycle_start≤today≤cycle_end` 조회→통장별 group by 조립) + `changeLineStatus`(@Transactional, 소유권 게이트→과거 사이클 잠금→전이). ④ `cycle/ChecklistResponse`(record — AccountGroup/Line/Progress 중첩, WaterfallResponse 패턴 동형, 서비스가 조립). ⑤ 엔티티/리포 확장: `PlanLine.changeStatus(status, Instant)`(처리 시 checked_at=주입 Clock, PENDING 복귀 시 null) + `checked_at` 매핑(기존 V1 컬럼, 그동안 미매핑)·getter, `CycleRepository.findByUserIdAndCycleStartLessThanEqualAndCycleEndGreaterThanEqual`(현재 사이클), `ErrorCode.CYCLE_LOCKED(409)`(신규).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **통합 9건**(`CycleChecklistIntegrationTest`, Testcontainers+MockMvc+실 JWT+가변 KST Clock, 노션 실데이터: 통장별 그룹[국민 700,000·기타통장 1,417,003·생활비통장 LIVING 356,107, 첫등장순서 보존]·진행도 7라인/done 0 → DONE 시 1 → checked_at=주입시각 / **건너뜀도 done에 집계**[PENDING 아닌 라인=처리됨] / PENDING 복귀 시 checked_at 비움 / 잘못된 상태값 400·미변경 / **타인 라인 404·미변경** / 부재 라인 404 / **지난 사이클(cycle_end+1) CYCLE_LOCKED 409·미변경** / 현재 사이클 경계 밖이면 404). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **CYCLE-06 → true**.
- 다음: **SCR-03b**(홈 체크리스트 카드, 프론트 — 지급일~D+3 노출, 실수령액 확인[CYCLE-04 `PATCH /cycles/{id}/income`]→통장별 체크/건너뜀[이번 `GET /cycles/current`·`PATCH /plan-lines/{id}`] 동선. 선행 백엔드 전부 준비됨)·**ITEM-07**(항목 수정 적용 시점 — 구현규칙 4장 재생성: DONE 보존·PENDING/SKIPPED 삭제 후 재생성, 이번 상태 전이 위에 얹힘). 그 외 미완료는 수동 **VERIFY-notion-match**. 실 운용엔 일일 판정 @Scheduled→createSnapshot 배선(CYCLE-03 후속)도 여전히 필요.
- 메모: ① **진행도 의미론 결정(owner 검토 요망)** — `progress.done` = **처리된(PENDING 아닌) 라인 수**(DONE+SKIPPED), total=전체. `done==total`이 "처리할 라인 없음" 신호가 되게 함(전부 SKIPPED인 체크리스트가 영원히 미완으로 보이는 것 회피). 필드명만 보면 DONE만 셀 여지도 있어 SCR-03b 동선과 함께 확인 권장. ② **CYCLE_LOCKED(409, 신규 ErrorCode)** — 과거 사이클(cycle_end 경과) 라인 변경 차단(구현규칙 2장 과거 불변). API명세에 명시된 코드는 아니고 네이밍 규칙(`도메인_상황`)만 따름. 미래 사이클은 잠그지 않음(스냅샷이 지급일 생성이라 cycle_start>today 라인은 정상 흐름에 없음). ③ **봉투 연동 제외(범위)** — 구현규칙 2장의 ENVELOPE 라인 EnvelopeTransaction 생성·`LINE_LOCKED_BY_SPEND`는 봉투 도메인(Phase 3)이 없어 대상 아님. 현재 plan_lines는 ITEM·LIVING뿐이라 전이는 상태·checked_at 갱신만. 봉투 도입 시 ENVELOPE 전이에 적립 부수효과 추가. ④ **소유권 비노출** — 라인→사이클→사용자 게이트, 미소유·부재 모두 NOT_FOUND(AccountService 패턴). 라인 단독 조회 후 `findByIdAndUserId(line.cycleId, userId)`로 교차. ⑤ **통장 그룹 순서** — plan_lines는 sort_order 미보유라 라인 id asc(≈항목 입력순)의 통장 첫 등장 순서를 LinkedHashMap으로 보존. ⑥ **checked_at 매핑 추가** — V1에 이미 있던 컬럼이나 엔티티 미매핑이었음(ddl-auto validate는 매핑된 컬럼만 검사). 이번에 매핑+getter 추가, 시각은 주입 Clock(규칙 3, Instant.now() 금지). ⑦ **Flyway 새 버전 없음**(cycles·plan_lines·checked_at 전부 V1 기존). **골든 새 케이스 없음**(체크리스트는 DB 상태 검증이라 골든 독립). ⑧ spotless가 신규 파일 자동 정리(spotlessApply 1회 후 verify 초록). JAVA_HOME=jdk-22 우회 여전. ⑨ 커밋 d500534.

## 2026-06-14  —  [CYCLE-04 완료] 실수령액 확인 — cycles.income_confirmed + LIVING 라인 재계산
- 한 일: 직전 CYCLE-03가 깐 cycles/plan_lines 스냅샷(income=base_income·income_confirmed=false 생성) 위에 **체크리스트 1단계 실수령액 확인**을 얹음(API명세 5장 `PATCH /cycles/{id}/income`). owner_only인 폭포 계산은 손대지 않고, 구현규칙 3장에 명시된 LIVING 갱신 공식만 적용. ① `cycle/CycleController` `PATCH /api/v1/cycles/{id}/income`(인증 필수, principal=userId, body `{income}` — `@Min(1)@Max(10억)` 구현규칙 5장 검증). 응답 `CycleResponse`(id·label·경계·income·incomeConfirmed). 여윳돈 배분 제안(CYCLE-05)은 Phase 6라 응답 제외. ② `cycle/CycleIncomeService`(@Service, application) — `@Transactional confirmIncome(userId, cycleId, income)`: 소유권 게이트(`findByIdAndUserId` orElseThrow NOT_FOUND, 존재 비노출) → `cycle.confirmIncome(income)`(income·income_confirmed=true) → **LIVING 재계산**. ③ 엔티티/리포 확장: `Cycle.confirmIncome(long)`(경계는 여전히 불변, 확인 실수령액·플래그만), `PlanLine.updatePlannedAmount(long)`, `CycleRepository.findByIdAndUserId`. 스케줄러/체크리스트 조회(CYCLE-06)는 후속.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **통합 6건**(`CycleIncomeIntegrationTest`, Testcontainers+MockMvc+실 JWT, 노션 실데이터 기준[항목합 2,117,003 고정]: 스냅샷 직후 income=평소금액·확인 전 / **상향 확정 시 플래그 on + LIVING 2,500,000−2,117,003=382,997 재계산·타 6라인 불변** / **하향으로 생활비 ≤0(2,000,000→−117,003) 시 LIVING 라인 제거**[6건 잔존·income·플래그는 갱신] / 생활비 통장 미지정 시 LIVING 없이 확인만[없던 라인 신규생성 안 함] / **타인 사이클 404 NOT_FOUND·미변경** / 0원 400 VALIDATION_FAILED·미변경). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **CYCLE-04 → true**.
- 다음: 위임 가능한 Phase 2 backend **CYCLE-06**(통장별 체크리스트 — plan_lines를 account_id로 group by 합산 + 라인 상태 전이 PENDING↔DONE/SKIPPED, API명세 GET /cycles/current·PATCH /plan-lines/{id}, 구현규칙 2장 봉투 연동·과거 사이클 불변)·**ITEM-07**(항목 수정 적용 시점 — 구현규칙 4장 재생성: DONE 보존·PENDING/SKIPPED 삭제 후 재생성). frontend **SCR-03b**(홈 체크리스트 카드)는 CYCLE-06까지 붙어야 동선 완성. 그 외 미완료는 수동 **VERIFY-notion-match**. 실 운용엔 일일 판정 @Scheduled→createSnapshot 배선(CYCLE-03 후속)도 여전히 필요.
- 메모: ① **LIVING 재계산 공식 = 구현규칙 3장 그대로**(LIVING = income − Σ(LIVING 외 라인)). 항목·EMERGENCY 라인은 스냅샷 고정값이라 실수령액 변동은 전부 생활비가 흡수 → **폭포 캐스케이드를 다시 돌리지 않고(owner 도메인 비침범) 잔액만 재산정**. WaterfallQueryService/CycleSnapshotService와 동형의 분담(이 서비스 계산 0줄에 가까움, 단순 차감). ② **PENDING일 때만 갱신(구현규칙 3장)** — DONE LIVING은 이미 이체된 사실이라 보존, 차액 EXTRA 라인 제안은 CYCLE-05(Phase 6) 소관이라 미구현. CYCLE-04 흐름(지급일 1단계)에선 LIVING이 갓 생성된 PENDING이라 항상 갱신 경로. ③ **≤0이면 LIVING 라인 제거(owner 검토 권장)** — 재계산 결과가 0 이하면 이체할 생활비가 없으므로 라인을 지워 FLOW-03의 "과배분/shortfall 시 LIVING 생략" 의미론과 일치시킴. 분배 의미론이라 owner 비준 여지. ④ **없던 LIVING 신규 생성 안 함** — 생활비 통장 미지정 등으로 LIVING 라인이 애초에 없으면 재계산 대상 없음(income·플래그만 갱신). 없던 라인을 새로 만드는 건 "이번 달 반영" 재생성(ITEM-07) 소관. ⑤ **소유권 NOT_FOUND 비노출**(AccountService 패턴 동형) — 미소유·부재 모두 NOT_FOUND. ⑥ **Flyway 새 버전 없음**(cycles.income·income_confirmed는 V1 기존). **골든 새 케이스 없음**(통합 테스트가 골든과 독립 입력으로 LIVING 산수를 교차 확인). ⑦ spotless가 신규 파일 자동 정리(spotlessApply 1회 후 verify 초록). JAVA_HOME=jdk-22 우회 여전. ⑧ 커밋 7280c48.

## 2026-06-14  —  [CYCLE-03 완료] 사이클 스냅샷 영속화 — cycles·plan_lines 멱등 적재 (owner_only 페어)
- 한 일: Phase 2의 관문이던 owner_only **CYCLE-03**를 페어 모드로 닫음(owner가 LIVING 스냅샷 의미론·산출물 범위 결정). 직전까지 재료는 다 있었으나(CYCLE-02 `CycleService.resolveCycle` 경계·라벨, FLOW-03 `CycleSnapshotBuilder`가 LIVING 356,107 골든 검증, `Cycle.create`/`CycleRepository.existsByUserIdAndCycleStart` 멱등 게이트) 영속화 오케스트레이션과 plan_lines 엔티티가 비어 있었음. ① `cycle/infra/PlanLine`(엔티티 — plan_lines 매핑, ddl-auto validate 통과: cycle_id·line_type·budget_item_id·envelope_id·account_id·name/category/account_name_snapshot·planned_amount·status·checked_at. `item()`/`living()` 팩토리, status=PENDING 생성). ② `cycle/infra/PlanLineStatus{PENDING,DONE,SKIPPED}`(닫힌 도메인, CYCLE-03은 PENDING만 적재·전이는 CYCLE-06). ③ `cycle/infra/PlanLineRepository`(`findByCycleIdOrderByIdAsc`). ④ `cycle/CycleSnapshotService`(@Service, application — `@Transactional createSnapshot(userId, startMonth)`: `CycleService`로 경계 도출 → **멱등 게이트** `findByUserIdAndCycleStart` orElseGet → 활성 항목(`findByUserIdAndStatusOrderBySortOrderAsc ACTIVE`)을 `WaterfallLine`으로 변환 → owner `CycleSnapshotBuilder.build`(계산 0줄) → `Cycle.create(income=base_income)` 저장 후 각 `PlanLineDraft`를 항목·통장으로 해석해 name/category/account_name 스냅샷 채운 `PlanLine`으로 saveAll). 스케줄러 트리거는 owner 결정으로 후속 분리.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **통합 5건**(`CycleSnapshotServiceIntegrationTest`, Testcontainers: **노션 실데이터 → plan_lines LIVING 356,107**[ITEM 6건 sort_order 보존·EMERGENCY=ITEM+category_snapshot 구분·name/account_name 스냅샷·cycleStart 2026-06-25·income_confirmed false] + **동일 사이클 재생성 멱등**[같은 id 반환·cycles 1건·plan_lines 7건 불변] + 생활비 통장 미지정 시 LIVING 미생성[6건] + 과배분(living≤0) 시 LIVING 미생성 + 빈 항목 시 LIVING만[income 전액]). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(62, 프론트 무변경).
- passes 전환: **CYCLE-03 → true**.
- 다음: CYCLE-03이 관문이던 Phase 2 항목들이 전부 풀림 — **CYCLE-04**(실수령액 확인 cycles.income_confirmed)·**CYCLE-06**(통장별 체크리스트 — plan_lines를 account_id로 group by + 상태 전이)·**ITEM-07**(수정 적용 시점 — 구현규칙 4장 재생성)·**SCR-03b**(홈 체크리스트 카드, 프론트) 전부 위임 가능. 그 외 미완료는 수동 **VERIFY-notion-match**. 실 운용엔 일일 판정 `@Scheduled`→`createSnapshot` 배선(아키텍처 4장 스냅샷→알림 순서, NOTI-01 판정 재사용)이 후속으로 필요.
- 메모: ① **owner 결정 2건(AskUserQuestion 확정)** — ⓐ LIVING 라인의 NOT NULL `name_snapshot`·`category_snapshot`은 **머신 토큰 "LIVING"** 저장(line_type과 동일, 프론트가 i18n 매핑 → 규칙 7 "서버는 문장 생성 금지" 준수). 엔티티에서 `PlanLineType.LIVING.name()`으로 self-documenting하게 박음(매직 스트링 회피). ⓑ 산출물 범위 = **서비스+엔티티+통합 테스트**까지, @Scheduled 트리거는 후속 분리(verify가 '멱등 영속화 통합 테스트'만 요구 + 아키텍처 스냅샷→알림 순서 배선은 owner 결정 존중). ② **account_name_snapshot NOT NULL 처리(결정)** — 통장은 soft delete(is_active=false)라 행이 남아 별칭 조회 가능(ERD "plan_lines 참조 유지"). 못 찾는 경우는 탈퇴 cascade 물리삭제뿐(정상 흐름 없음)이라, 손상 스냅샷 대신 `IllegalStateException`으로 불변식 위반을 끊음. 생활비 통장은 항목이 없을 수 있어 accountIds에 따로 더함. ③ **멱등 2중** — `findByUserIdAndCycleStart` orElseGet(정상 경로, 단일 인스턴스 ADR-04엔 충분) + cycles `unique(user_id, cycle_start)`(경합 최종 가드, CYCLE-02 통합 테스트가 이미 직접 검증). 재실행 시 기존 Cycle 반환·plan_lines 미적재. ④ **계산 0줄 분담** — `WaterfallQueryService`(FLOW-02)와 동형으로 경계는 CycleService, 폭포·LIVING 조건은 owner `CycleSnapshotBuilder`에 위임, 이 서비스는 draft→엔티티 해석·영속화만. ⑤ **Flyway 새 버전 없음**(plan_lines·cycles는 V1 기존, FK·unique 포함). **골든 새 케이스 없음**(통합 테스트가 독립 입력으로 골든과 같은 356,107 교차 확인, 골든 파일 독립 — 메모리 `golden-fixture-needs-owner-lock` 준수). ⑥ spotless가 신규 파일 자동 정리(spotlessApply 1회 후 verify 초록). JAVA_HOME=jdk-22 우회 여전.

## 2026-06-14  —  [NOTI-05 완료] 이메일 알림 채널 어댑터 — NotificationSender EMAIL 구현 + 채널 config 선택
- 한 일: 직전 NOTI-04 메모 ⑤가 명시적으로 예고한 "채널 교체 지점"(데코레이터 delegate가 concrete `LoggingNotificationSender` 주입이라 NOTI-05가 채널 교체 시 인터페이스+@Qualifier 디커플링 검토)을 닫음. `NotificationSender` 포트의 **EMAIL 구현**을 추가하고 실 SMTP 발송은 포트 뒤로 숨김(ADR-05 "알림 채널만 포트"). ① `notification/EmailNotificationSender`(@Component **@RealChannel** **@ConditionalOnProperty(app.notification.channel=email)**) — `send(type,userId,date)`가 `UserRepository.findById`로 수신자 조회 → **이메일 미수집(동의 거부, ERD users.email NULL)이면 조용히 스킵**(return), 있으면 `MailClient.send(email, locale, type, date)` 호출. 발송 실패(MailClient 예외)는 **그대로 전파** → NOTI-04 게이트의 REQUIRES_NEW가 롤백돼 기록 미적재 → 다음 배치 재시도. `channel()`→EMAIL. **제목·본문 문장은 만들지 않고 구조화 데이터만 넘김(규칙 7)** — locale은 실 어댑터가 메시지 번들(ko/en, SET-03) 렌더링에 사용. ② `notification/MailClient`(포트 — verify가 요구한 "모킹할 발송 인터페이스"). ③ `notification/RealChannel`(@Qualifier 메타 애노테이션 — 게이트가 @Primary라 `NotificationSender` 타입으론 자기 자신이 잡히는 문제 해결, 감쌀 실채널을 명시). ④ `NotificationChannel`에 **EMAIL** 추가(LOG 옆). ⑤ `DeduplicatingNotificationSender` delegate를 concrete `LoggingNotificationSender`→`@RealChannel NotificationSender`로 **디커플링**(메모 ⑤ 예고대로, 그 외 로직 무수정). ⑥ `LoggingNotificationSender`에 `@RealChannel` + `@ConditionalOnProperty(channel=log, matchIfMissing=true)`. ⑦ `application.yml app.notification.channel`(NOTIFICATION_CHANNEL, **기본 log**=SMTP 불요, email은 옵트인).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **단위 6건**(`EmailNotificationSenderTest`: 이메일 있는 사용자 구조화 데이터 발송[email·locale·type·date 인자 검증]·이메일 null 스킵·공백 스킵·MailClient 예외 전파·대상 사용자 없음 IllegalStateException·channel=EMAIL) + **통합 2건**(`EmailNotificationChannelIntegrationTest`, Testcontainers + `@TestPropertySource(channel=email)` + `@MockitoBean MailClient`: 게이트가 EMAIL로 발송하고 notification_logs에 channel=EMAIL 기록·미수집 사용자는 발송 없이 기록만[멱등]). **NOTI-04 게이트 통합 테스트는 기본 log 채널로 무수정 통과**(@RealChannel이 LoggingNotificationSender spy로 해석). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **NOTI-05 → true**.
- 다음: 위임 가능한 독립 항목 **사실상 소진**. Phase 2 나머지(**CYCLE-04** 실수령액 확인·**CYCLE-06** 통장별 체크리스트·**ITEM-07** 항목 수정 적용 시점·**SCR-03b** 홈 체크리스트 카드)는 전부 owner_only **CYCLE-03 스냅샷 영속화**(cycles/plan_lines 멱등 적재 — CycleService.resolveCycle + CycleSnapshotBuilder를 박으면 됨)에 의존 → **CYCLE-03가 다음 관문**(페어). 그 외 미완료는 수동 **VERIFY-notion-match**(실 OAuth/브라우저). NOTI-05 실 발송을 켜려면(이번엔 미구현) MailClient(JavaMailSender) 어댑터 + SMTP 설정 + 메시지 번들(ko/en) 추가가 별도 작업.
- 메모: ① **범위 결정(추천안 채택, owner 승인)** — NOTI-05 verify는 "이메일 발송 어댑터 호출·실패 처리 테스트(발송 인터페이스 모킹)"이므로 **어댑터+포트+테스트+채널 config 선택**까지로 정의 충족. 실 JavaMail/SMTP 인프라는 **미구현**(MailClient 실 구현 없음 — 운영 SMTP 키·메시지 번들이 필요한 별도 작업). `email` 채널을 켜려면 MailClient 빈+SMTP가 함께 필요하고, 없으면 컨텍스트가 뜨지 않으므로 **기본 log**로 둬 부팅·배치 무손상. 고아 회피 원칙과 충돌 안 함 — EmailNotificationSender는 NOTI-05의 명시적 산출물(ADR-05 포트의 EMAIL 구현)이고 config로 활성화 가능. ② **@RealChannel 디커플링(메모 ⑤ 이행)** — 게이트가 @Primary라 `NotificationSender` 타입 주입은 자기 자신이 잡힘 → 감쌀 실채널을 @Qualifier로 명시. Logging/Email은 `@ConditionalOnProperty`로 **동시 존재 안 함**(log matchIfMissing=true / email은 havingValue=email)이라 @RealChannel 빈은 항상 정확히 하나. ③ **NOTI-04 무손상 확인** — 기존 `DeduplicatingNotificationSenderIntegrationTest`는 `@MockitoSpyBean LoggingNotificationSender`로 채널을 감시하고 channel=LOG를 단언하는데, 기본 채널이 log라 @RealChannel이 그 spy로 해석돼 데코레이터가 spy를 감쌈 → **테스트 무수정 통과**(디커플링이 기존 검증을 안 깸). ④ **미수집 사용자 기록 의미론(결정)** — email NULL이면 EmailNotificationSender는 no-op이지만 게이트는 "기록 먼저"라 notification_logs 행(channel=EMAIL)이 남는다. 보낸 적 없는 알림을 기록하는 사소한 흠이 있으나, **매일 재시도 폭주를 막는 멱등**이 더 중요(예외를 던지면 무한 일일 재시도). 통합 테스트가 이 동작을 명시 검증. ⑤ **rule 7** — 어댑터는 문장 0개, 구조화 데이터(email·locale·type·date)만 MailClient에 전달. 내용 렌더링은 실 어댑터가 locale로 메시지 번들 선택(미구현). ⑥ **Flyway 새 버전 없음**(notification_logs.channel은 V1 기존 varchar, EMAIL은 enum 값 추가일 뿐). **골든 새 케이스 없음**(채널 어댑터는 골든과 독립). ⑦ spotless가 신규 파일 자동 정리(spotlessApply 1회 후 verify 초록). JAVA_HOME=jdk-22 우회 여전. ⑧ 커밋 593145a.

## 2026-06-14  —  [NOTI-04 완료] 알림 중복 방지 — notification_logs 멱등 게이트로 동일 알림 1회만 발송
- 한 일: 직전 NOTI-01이 명시적으로 남긴 멱등 구멍(메모 ④ "자체는 발송 로그를 쓰지 않아 같은 날 재실행 시 재발송 — NOTI-04 소관")을 닫음. NOTI-01의 `NotificationSender` 포트를 감싸는 데코레이터로 도입(아키텍처 4장 "기록 먼저, 발송 후 확정"). ① `notification/DeduplicatingNotificationSender`(@Component **@Primary**, application) — 실제 채널(`LoggingNotificationSender`, NOTI-05가 대체)을 감싸 `send()` 시 ⓐ `existsByUserIdAndTypeAndTargetDate`면 스킵(정상 경로 멱등) ⓑ 없으면 `notification_logs` 기록 적재 → 채널 발송 → 커밋 확정. `@Transactional(propagation=REQUIRES_NEW)`로 사용자별 독립 쓰기 트랜잭션(호출자 `PaydayNotificationService`가 readOnly여도 동작) — 발송 예외 시 이 트랜잭션이 롤백돼 기록이 남지 않아 다음 일일 배치에서 **재시도**(중복 발송보다 누락-후-재시도를 택함). @Primary라 `NotificationSender` 주입처(PaydayNotificationService)는 **무수정**으로 게이트를 거친다. ② `notification/infra/NotificationLog`(엔티티 — notification_logs 매핑: user_id·type·target_date·channel·sent_at) + `NotificationLogRepository`(`existsByUserIdAndTypeAndTargetDate` + JpaRepository). sent_at은 주입 `Clock.instant()`(규칙 3, `Instant.now()` 금지 — checkstyle). ③ `notification_logs.channel`(NN) 채우려 `NotificationSender`에 `channel()` 추가 + `notification/NotificationChannel{LOG}` enum(v1 유일 채널=로그 자리표시, EMAIL/WEB_PUSH/FCM은 NOTI-05/Phase8이 도입 시 추가 — `NotificationType` 고아 값 회피 패턴 동형). `LoggingNotificationSender.channel()`→LOG.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **통합 6건**(`DeduplicatingNotificationSenderIntegrationTest`, Testcontainers + `@MockitoSpyBean`으로 채널 호출 횟수 직접 확인: 첫 발송 시 채널 1회·기록 적재[channel=LOG·sent_at=주입 clock]·**동일 알림 재발송 스킵**[채널 1회·행 1건]·대상일 다르면 각각 발송·**기록 선재 시 채널 미호출**[never]·**발송 실패 시 기록 롤백 후 재시도**[count 0→재시도 시 채널 times(2)·count 1]·**unique 제약 직접 차단**[DataIntegrityViolationException]). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **NOTI-04 → true**.
- 다음: **NOTI-05 채널 어댑터**(이메일/웹푸시 실 `NotificationSender` 구현 — 이번에 추가한 `channel()`로 EMAIL 반환, `NotificationChannel`에 EMAIL 추가, `LoggingNotificationSender` 대체. 데코레이터 delegate가 현재 `LoggingNotificationSender` **concrete 주입**이라 NOTI-05가 채널 교체 시 이 delegate 타입/주입을 함께 갱신해야 함 — 메모 ⑤ 참조). 또는 owner_only **CYCLE-03 스냅샷 영속화**(Phase 2 핵심 관문 — CYCLE-04/06·ITEM-07·SCR-03b가 의존). NOTI-05는 CYCLE-03 없이 착수 가능.
- 메모: ① **@Primary 충돌로 NOTI-01 테스트 전환(결정)** — main에 `DeduplicatingNotificationSender`(@Primary)를 두면 NOTI-01 통합 테스트의 `@Primary RecordingNotificationSender`와 **두 @Primary 충돌**(PaydayNotificationService 주입 모호). 그래서 NOTI-01 테스트의 RecordingSender/Config를 제거하고 **발송 기록(notification_logs) 기반 검증**으로 전환(디스패치 대상 = 적재된 로그 행, 의도·6케이스 보존 + `notified` count 단언 그대로). 데코레이터가 실 `LoggingNotificationSender`를 거쳐 행을 적재하므로 충실한 증거. ② **REQUIRES_NEW 선택(결정)** — 호출자 readOnly 트랜잭션에 join하면 쓰기 불가 + 사용자별 원자성("기록+발송"이 함께 커밋/롤백)이 "발송 후 확정" 의미론과 정확히 일치. 발송 실패 시 그 사용자만 롤백, 루프는 계속. ③ **멱등 2중** — `existsBy`(정상 경로 스킵, 단일 인스턴스 ADR-04에선 이걸로 충분) + `unique(user_id,type,target_date)`(경합/다중 실행 시 DB 레벨 최종 가드, 통합 테스트가 직접 검증). ④ **channel() 포트 추가 정당성** — notification_logs.channel은 NOT NULL인데 데코레이터는 임의 delegate를 감싸므로 채널을 스스로 알 수 없음 → 채널이 자신을 밝히는 `channel()`이 올바른 자리. `NotificationChannel{LOG}`만 둠(ERD 예시 EMAIL/WEB_PUSH/FCM은 varchar라 제약 아님, v1 실제 채널은 로그뿐 — 고아 값 회피). ⑤ **delegate concrete 주입(주의)** — 데코레이터가 `LoggingNotificationSender`를 **concrete 타입으로 주입**(인터페이스+@Primary면 자기 자신 순환/모호). 단순하지만 NOTI-05가 채널 교체 시 delegate 타입·주입을 함께 바꿔야 함(인터페이스+@Qualifier로 디커플링은 NOTI-05 도입 시 검토). ⑥ **테스트 FK 함정** — notification_logs.user_id는 users FK(ON DELETE CASCADE)라, 로그 행 직접 삽입 테스트는 실제 User를 먼저 생성해 그 id를 써야 함(가짜 1001L로 처음 작성했다 FK 위반으로 6건 동시 실패 → setUp에서 User 생성으로 교정). ⑦ **Flyway 새 버전 없음**(notification_logs는 V1 기존, unique 제약 포함). **골든 새 케이스 없음**(중복 방지는 DB 상태 검증이라 골든 파일과 독립). ⑧ spotless가 신규 파일 자동 정리(spotlessApply 1회). JAVA_HOME=jdk-22 우회 여전. ⑨ 커밋 14aeda9.

## 2026-06-14  —  [NOTI-01 완료] 지급일 알림 — 일일 판정으로 실지급일 사용자에게만 발송
- 한 일: 직전 progress.md가 "owner 작업(CYCLE-03) 없이 위임 착수 가능한 후보"로 지목한 NOTI-01을 닫음. 기존 `PaydayService`(CYCLE-01) 위에 **지급일 알림 판정·발송**을 얹음(아키텍처 4장 NotificationStep의 PAYDAY 부분). 신규 `notification` 패키지: ① `notification/PaydayNotificationService`(@Service, application) — `notifyPaydays()`가 온보딩 완료 사용자를 돌며 "오늘이 실제 지급일인가"를 `PaydayService.resolveActualPayday`로 판정해 대상자에게만 `NotificationSender`로 PAYDAY 디스패치(계산 0줄, 기준일은 주입 `Clock`=규칙 3, 쓰기 없음 readOnly). ② `notification/NotificationSender`(포트, `send(type, userId, targetDate)`) + `notification/LoggingNotificationSender`(@Component, v1 기본 채널=로그). ③ `notification/infra/PaydayNotificationScheduler`(@Scheduled 매일 04:00 KST, 얇은 트리거 — 기존 Holiday/Maturity 스케줄러와 동형). ④ `UserRepository.findByBaseIncomeGreaterThan(long)` — 온보딩 완료(base_income>0) 사용자만 조회. 채널 어댑터(NOTI-05)·중복 방지(NOTI-04)는 미구현 = 각 항목 소관.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **통합 6건**(`PaydayNotificationServiceIntegrationTest`: 실지급일자만 대상[A 발송·B 미발송]·비지급일 전원 미발송·**월말+NEXT 다음달 경계**[31일 NEXT 1/31토→2/2월, 2/2 발송·명목일 1/31 미발송]·**공휴일 회피**[1일 NEXT 신정 1/1→1/2, 1/2 발송·1/1 미발송]·**온보딩 전 플레이스홀더 제외**[base_income=0]). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **NOTI-01 → true**.
- 다음: **NOTI-04 알림 중복 방지**(이번 `NotificationSender` 포트를 감싸 notification_logs `unique(user_id, type, target_date)` "기록 먼저·발송 후 확정" 멱등 게이트 도입 — 위임 가능, NOTI-01의 짝) → **NOTI-05 채널 어댑터**(이메일/웹푸시 실 `NotificationSender` 구현, LoggingNotificationSender 대체). 또는 owner_only **CYCLE-03 스냅샷 영속화**(Phase 2 핵심 관문 — CYCLE-04/06/ITEM-07/SCR-03b가 의존). NOTI-04/05는 CYCLE-03 없이도 착수 가능.
- 메모: ① **3-후보 월 판정(결정)** — 영업일 조정이 월 경계를 넘으면(말일+NEXT→다음 달 초, 1일+PREV→전월 말) 명목 월급일이 속한 달과 실지급일이 떨어지는 달이 어긋난다. 그래서 오늘 기준 **전월·당월·익월** 세 명목 월의 실지급일을 보고 하나라도 오늘과 같으면 지급일로 본다. 지급일은 ~한 달 간격이라 둘 이상이 동시에 오늘과 같을 수 없어 중복 발화 없음(통합 테스트의 1/31↔2/2, 1/1↔1/2 케이스가 이 경계를 교차 검증). ② **범위 분리(NOTI 3분할)** — NOTI-01=판정+디스패치, **NOTI-04=중복 방지**(notification_logs 기록 우선 멱등 게이트), **NOTI-05=채널 어댑터**(이메일). 포트 시그니처 `send(type, userId, targetDate)`를 NOTI-04의 `unique(user_id, type, target_date)` 키에 미리 맞춰 둠. CYCLE-01→02, FLOW-03→CYCLE-03 분리 전례와 동형. ③ **온보딩 전 제외** — `createFromOAuth` 플레이스홀더(base_income=0, payday=1)는 체크리스트가 없어 알림 대상이 아님 → `findByBaseIncomeGreaterThan(0)`로 DB에서 거른다. ④ **멱등 주의** — NOTI-01 자체는 발송 로그를 쓰지 않아 같은 날 재실행 시 재발송된다(규칙 8 멱등 게이트는 NOTI-04 소관, 아키텍처 "기록 먼저·발송 후 확정"). 스케줄러는 하루 1회라 실 운용엔 무해하나, 멱등은 NOTI-04에서 닫아야 함. ⑤ **스케줄러 순서** — 아키텍처 4장은 알림을 스냅샷(CYCLE-03) 뒤에 두지만, 현재는 Holiday/Maturity와 마찬가지로 독립 @Scheduled(04:00 KST, ADR-04 단일 인스턴스). CYCLE-03 스냅샷 단계가 들어오면 순서 보장 통합 러너로 묶을 수 있음. ⑥ **Flyway 새 버전 없음**(notification_logs는 V1 기존, NOTI-01은 미사용). **골든 새 케이스 없음**(통합 테스트가 DB 주입 공휴일·달력으로 PaydayResolver 골든과 같은 의미를 교차 확인). ⑦ 테스트는 가변 KST `MutableClock`(@Primary, setToday로 날짜 교체)으로 여러 기준일을 한 클래스에서 결정론 검증 + `RecordingNotificationSender`(@Primary)로 발송 호출 기록. spotless가 신규 파일 자동 정리. JAVA_HOME=jdk-22 우회 여전. ⑧ 커밋 2d6bfab.

## 2026-06-14  —  [CYCLE-02 완료] 사이클 정의 — 실지급일~다음 지급일 전날 경계·시작월 라벨 산출
- 한 일: 직전 CYCLE-01(공휴일 캐싱+`PaydayService.resolveActualPayday`) 위에 **사이클 경계 정의**를 얹음. owner_only인 "사이클 스냅샷 생성"(CYCLE-03)은 손대지 않고, 위임 가능한 **경계·라벨 산출 + 엔티티 매핑**만 구현. ① `cycle/domain/CycleResolver`(의존성 0 순수, CYCLE-02) — `resolve(YearMonth startMonth, payday, adjustment, Set<LocalDate> holidays)`가 `PaydayResolver`로 **시작 월·다음 달 두 실지급일**을 도출 → `cycleStart`(실지급일)·`cycleEnd`(다음 지급일−1)·`label`(시작 월 "yyyy-MM"). ② `cycle/domain/CycleDefinition`(record — cycleStart/cycleEnd/label + end≥start 불변식). ③ `cycle/CycleService.resolveCycle`(application — `HolidayCalendar`로 **두 달 창의 공휴일을 합집합으로 주입**, 계산 0줄. 다음 달 조정이 그 다음 달까지 닿을 수 있어 `holidaysAround(startMonth)∪holidaysAround(startMonth+1)`). ④ `cycle/infra/Cycle`(엔티티 — cycles 매핑: user_id·cycle_start·cycle_end·label·income·income_confirmed[생성 시 false]) + `cycle/infra/CycleRepository`(`existsByUserIdAndCycleStart`·`findByUserIdAndCycleStart` — `unique(user_id, cycle_start)` 멱등 제약). 스냅샷 영속화(plan_lines 적재)는 미구현 = CYCLE-03 소관.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 **단위 7건**(`CycleResolverTest`: 실지급일~전날 경계·연속 두 사이클 맞닿음·말일 clamp+PREV[2/28토→2/27·다음달 3/30]·공휴일 회피[NEXT 1/1신정→1/2]·연휴[1/1·1/2+주말→1/5]·**전월 밀림에도 라벨은 명목 시작월**[1일+PREV+신정→start 2025-12-31·label "2026-01"]·검증) + **통합 5건**(`CycleServiceIntegrationTest`: 공휴일 주입 경계·캐시 빈 폴백[주말 회피만]·연속 경계·월말 clamp·**엔티티 저장+동일 cycle_start 재저장 시 DataIntegrityViolation**). 기존 단위·골든·ArchUnit·통합 전부 유지, **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **CYCLE-02 → true**.
- 다음: **CYCLE-03 스냅샷 영속화**(owner_only — FLOW-03 `CycleSnapshotBuilder`가 산출한 `PlanLineDraft`를 항목·통장으로 해석해 cycles/plan_lines에 멱등 적재. 이번 `CycleService.resolveCycle`로 경계 도출 + `Cycle.create`/`CycleRepository.existsByUserIdAndCycleStart`로 멱등 게이트). 이후 CYCLE-04(실수령액 확인)→CYCLE-06(통장별 체크리스트)→NOTI-01/04/05→SCR-03b. **NOTI-01**(지급일 일일 판정)은 이번 `CycleService`/`PaydayService`를 그대로 재사용하므로 owner 작업(CYCLE-03) 없이도 위임 착수 가능한 후보.
- 메모: ① **label 의미론 결정(owner 검토 요망)** — label은 **명목 시작 월**(`resolve`에 넘긴 `startMonth.toString()`)이지 실제 cycle_start의 달력 월이 아니다. 이유: PREV 조정이 전월로 밀리는 드문 경우(월급일 1일+신정 → 전월 말일)에 **연속한 두 사이클이 같은 라벨을 갖는 충돌을 피함**(실제-월 기준이면 12월 사이클과 1월 사이클이 둘 다 "2025-12"가 됨). API명세 예시(cycleStart 2026-06-25 → label "2026-06")와도 일치. CYCLE-03이 이 라벨을 저장·CYCLE-04/리포트가 표시하므로 의미론 확정 필요. ② **공휴일 주입 범위** — 경계는 두 달의 지급일을 보므로 `holidaysAround`를 두 번 합집합(시작월·다음달). 단일 호출(전월~익월)로는 다음 달 지급일의 조정 구간을 못 덮을 수 있어 합쳤다. ③ **범위 경계** — CYCLE-02는 "정의"(경계·라벨)이고 영속화·plan_lines·멱등 생성 오케스트레이션은 owner_only CYCLE-03. 단 `Cycle` 엔티티·리포지토리는 owner가 CYCLE-02로 명시 스코프(요구사항·feature_list)했고 위임 가능 보일러플레이트라 함께 작성, 멱등 제약 동작은 통합 테스트로 검증(스냅샷 생성 로직은 미작성). ④ **Clock 미사용** — `resolve`는 호출자가 `startMonth`를 주입하는 순수 함수라 `LocalDate.now()` 호출 없음(규칙 3). "오늘이 속한 사이클" 판정은 Clock 주입이 필요한 CYCLE-03/NOTI 소관. ⑤ **Flyway 새 버전 없음**(cycles 테이블은 V1에 이미 존재, FK·unique 포함). ⑥ **골든 새 케이스 없음** — 경계는 PaydayResolver 단위·골든이 이미 덮는 실지급일을 조합할 뿐, 통합 테스트가 DB 주입 공휴일로 교차 확인. ⑦ spotless가 신규 테스트 줄바꿈 자동 정리(spotlessApply verify 한 번). JAVA_HOME=jdk-22 우회 여전. ⑧ 커밋 258df74.

## 2026-06-14  —  [CYCLE-01 완료] 공휴일 특일 API 캐싱 + 실지급일 산출 적용 (+ Phase 2 백로그 등재)
- 한 일: 세션 시작 시 Phase 0–1 위임 항목이 전부 소진(트래커 미완료는 수동 VERIFY-notion-match뿐)이라, 먼저 **Phase 2 항목 10개를 feature_list에 등재**(ITEM-07, CYCLE-01/02/03/04/06, NOTI-01/04/05, SCR-03b — 요구사항정의서 2.5/2.9·화면흐름도 SCR-03 근거, 커밋 ebd472f). 이어 위임 가능한 첫 항목 **CYCLE-01** 구현. owner 영역인 순수 `PaydayResolver`는 이미 완료(직전 HARNESS-golden 세션)라 손대지 않고 **위임 가능한 공휴일 infra + 적용 서비스**만 얹음: ① `cycle/infra/Holiday`+`HolidayRepository`(holidays 테이블, V1 기존 — ddl-auto validate 통과). ② `cycle/HolidayCalendar`(캐시 공휴일을 전월1일~익월말 구간 조회→`Set<LocalDate>`, 비면 빈 집합). ③ `cycle/PaydayService.resolveActualPayday(YearMonth,payday,adjustment)`(공휴일 주입+`PaydayResolver` 위임, **계산 0줄** — 캐시 비면 주말 회피만 = 폴백이 설계상 공짜). ④ `cycle/HolidayApiClient`(포트)+`cycle/infra/RestClientHolidayApiClient`(특일 `getRestDeInfo` 월별 조회·`isHoliday=Y` 추림·items 0/1/다건 방어 파싱, `build(true)`로 serviceKey 재인코딩 차단). ⑤ `cycle/HolidayCacheService.ensureCached(year)`(`source_year` 있으면 스킵·날짜 중복 스킵=멱등 규칙8, 외부 장애 시 예외 삼키고 캐시 유지=폴백). ⑥ `cycle/infra/HolidayRefreshScheduler`(매일 04:00 KST 올해·내년 확인=구현규칙 8장 1단계, 기준 연도는 주입 `Clock`=규칙3). ⑦ `cycle/CycleConfig`(RestClient·포트 빈)+`HolidayProperties`(`app.holiday`). ⑧ `application.yml app.holiday`(서비스 키 환경변수, 미주입 시 폴백).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 통합 **8건**(`PaydayServiceIntegrationTest` 4: 공휴일 주입 이동[2026-1/1 신정→1/2]·캐시 빈 폴백[1/1 평일 유지]·주말 회피[1/3토→1/5월]·말일 clamp+PREV[2/31→2/28토→2/27금] / `HolidayCacheServiceIntegrationTest` 4: 미캐싱 수집 적재·연1회 멱등 재수집 스킵·날짜 중복 스킵·API 장애 폴백[예외 삼킴·캐시 빔]) + 기존 단위·골든·ArchUnit·통합 전부 유지. **골든 fixture(폭포·만기·payday) 무수정 통과**. `npm run verify` 통과(프론트 무변경).
- passes 전환: **CYCLE-01 → true**.
- 다음: **CYCLE-02 사이클 정의**(cycles 엔티티 + 실지급일~다음 지급일−1 경계·시작월 라벨 — 이번 `PaydayService`로 경계 도출, 위임 가능) → **CYCLE-03 스냅샷 영속화**(owner_only — FLOW-03 `CycleSnapshotBuilder`+이번 경계를 cycles/plan_lines에 멱등 적재). 이후 CYCLE-04/06(체크리스트)→NOTI-01/04/05→SCR-03b. NOTI-01(지급일 알림 일일 판정)도 이번 `PaydayService` 재사용.
- 메모: ① **공휴일 주입 결정 재확인** — PaydayResolver는 `Set<LocalDate>`를 **인자로** 받는 순수 함수(아키텍처 2장). CYCLE-01은 그 위에 캐싱·폴백·적용 application을 얹은 것뿐. **"빈 캐시 = 주말 회피만"이라 차년도 미제공·API 장애 폴백이 별도 분기 없이 공짜로 모델링됨**(메모리 `local-env`와 무관, 설계 결정). ② **특일 API는 포트 뒤에** — `HolidayApiClient` 인터페이스로 두고 테스트는 `@Primary` 스텁으로 대체(실 HTTP·서비스 키 불요, OAuthClient 통합 패턴 동형). 실 클라이언트(`getRestDeInfo`)는 월별 1~12 루프로 호출(특일 API는 solYear+solMonth 단위). ③ **멱등 2단** — 연도 단위(`existsBySourceYear`)로 "연 1회" + 날짜 단위(`existsByHolidayDate`)로 unique 충돌 회피. 스케줄러는 매일 돌지만 캐싱된 해는 스킵이라 실제 수집은 연 1회. ④ **Flyway 새 버전 없음** — holidays 테이블은 V1에 이미 존재. ⑤ **골든 새 케이스 없음** — payday 골든은 PaydayResolver 단위가 이미 덮고, 이번 통합 테스트는 DB 주입 공휴일로 같은 의미를 교차 확인(골든 파일 독립). ⑥ spotless가 신규 파일 LF/줄바꿈 자동 정리(`spotlessApply verify` 한 번). JAVA_HOME=jdk-22 우회 여전. ⑦ owner_only 표시(feature_list): CYCLE-03만 true, CYCLE-01/02는 PaydayResolver 코어 완료라 잔여 infra/경계 산출은 위임 가능으로 분류.

## 2026-06-14  —  [HARNESS-golden 완료] PaydayResolver 실지급일 산출 + payday 골든 fixture (owner_only 페어)
- 한 일: Phase 0 마지막 잔여 항목. 골든 하네스는 이미 가동(maturity·waterfall 2 fixture lock 완료)이었고 **payday(실지급일) fixture 하나**만 비어 있었음 — 그런데 그 전제인 `PaydayResolver`가 미구현이라(아키텍처.md 도메인 계산기 4개 중 유일하게 없었음) owner_only 페어로 진행. ① `cycle/domain/PaydayResolver`(의존성 0 순수, CYCLE-01) — `resolve(YearMonth, payday, PaydayAdjustment, Set<LocalDate> holidays)`. **2단계: ① 말일 clamp**(해당 월에 없는 날짜→`month.lengthOfMonth()`) **② 영업일 조정**(NONE=무조정 / PREV·NEXT=영업일까지 ±1일, while 루프로 연휴·주말 연속 스킵, 월 경계 넘음). 영업일=토·일 아님 && holidays 미포함. ② 골든 `payday-cases.json`(**4묶음 16 케이스**: 말일 clamp·주말 회피[PREV/NEXT/NONE]·공휴일·연휴 끼임·차년도 미제공 폴백) + `golden/PaydayGoldenTest`(Jackson). ③ 단위 `cycle/domain/PaydayResolverTest` 11건(경계·윤년·월경계·검증·null). 모든 기대 날짜는 2026 실달력 요일을 PowerShell로 교차검증.
- 초록불: owner가 `./gradlew goldenLock`(payday 해시 manifest 등록, 에이전트 deny라 owner 직접 실행) 후 `./gradlew verify` **BUILD SUCCESSFUL** — 골든 16 + 단위 11 + 기존 전부 통과. lock 전엔 `GoldenFixtureIntegrityTest`만 빨간불(= 정상 핸드오프 게이트)이었고 lock 후 초록. `npm run verify`도 초록(프론트 무변경).
- passes 전환: **HARNESS-golden → true**. (이로써 **Phase 0 전부 완료**. feature_list 미완료는 이제 수동 **VERIFY-notion-match** 하나뿐.)
- 다음: ① **VERIFY-notion-match**(수동 — 브라우저 폭포 단계별 대조, 실 OAuth/JWT 필요) ② Phase 2 관문 = owner_only **CYCLE-01/02/03**(사이클 정의·실지급일 적용·멱등 스냅샷 영속화) — 이번 PaydayResolver를 그대로 호출 + FLOW-03의 CycleSnapshotBuilder를 cycles/plan_lines에 박으면 됨. ③ Phase 2 항목은 아직 feature_list 미등재(현 트래커는 Phase 0–1만) — 착수 전 백로그 확장 필요.
- 메모: ① **공휴일 주입 결정(owner 비준)** — PaydayResolver가 공휴일을 `Set<LocalDate>` **인자로** 받음(HolidayProvider 인터페이스 주입 아님). 이유: 순수 함수가 가장 합성 가능·골든 결정적, API/캐시·폴백 정책은 application/infra 책임. **빈 Set = 주말 회피만** → CYCLE-01 "차년도 미제공·API 장애 폴백"이 설계상 공짜로 모델링됨. 추상화가 필요하면 infra provider가 아니라 도메인 순수 `HolidayCalendar` 인터페이스가 올바른 자리(현재는 YAGNI). ② **알고리즘 결정 2건** — ⓐ clamp-then-adjust 순서(명목일 clamp 후 영업일 이동) ⓑ 조정이 월 경계 넘음 허용(2/28토+NEXT→3/2월). ③ **골든 CI 함정 회피** — 새 fixture가 LF인지 lock 전 확인(`.gitattributes` golden/** eol=lf), manifest 해시 `bae4a0b5…`는 LF 기준이라 Linux CI도 초록(메모리 `golden-hash-crlf-breaks-linux-ci` 준수). ④ **Jackson 쓰는 골든 테스트는 golden 패키지**(..domain.. 밖, ArchUnit 테스트 스캔 회피 — 메모리 `archunit-scans-test-classes`). cross-domain `cycle.domain→user.domain.PaydayAdjustment`는 framework 의존 아니라 ArchUnit 통과(WaterfallLine→Category와 동형). ⑤ Flyway 새 버전 없음(순수 계산 도메인). ⑥ spotless가 신규 파일 자동 정리(spotlessApply 1회). ⑦ owner가 goldenLock 직접 실행한 페어(FLOW-01 운영과 동일).

## 2026-06-14  —  [HARNESS-golden 수정] backend CI 빨간불 해소 — waterfall 골든 해시 LF 교정
- 한 일: FLOW-03 푸시 후 GitHub Actions에서 **backend(verify)가 실패**해 원인 추적. 제 커밋이 아니라 **FLOW-01 골든 도입(7aa47e8) 이후 backend CI가 줄곧 빨간불**(이후 doc/frontend 커밋만 이어져 안 드러남)이었음. 원인: `golden.sha256`의 `waterfall-cases.json` 해시가 **CRLF 기준(0f7b…)**으로 잠겨 있었음 — owner가 CRLF 워킹트리에서 goldenLock한 탓. 커밋된 정본 fixture는 LF(f9a6…)이고 CI는 LF로 체크아웃 → GoldenFixtureIntegrityTest 해시 불일치로 실패(로컬 Windows는 CRLF 워킹트리라 통과해 안 보임). 조치: ① 워킹트리 fixture를 LF로 재정규화(`tr -d '\r'`, 인덱스가 이미 LF라 git diff 없음) ② manifest의 waterfall 해시 0f7b…→**f9a6…(정본 LF 해시)** 한 줄 교정(owner 승인 — fixture 숫자 불변, 줄바꿈 교정일 뿐).
- 초록불: 로컬 `./gradlew clean verify` BUILD SUCCESSFUL + **GitHub Actions backend·frontend 둘 다 success**(커밋 33ef2f1) 확인. 골든 fixture 내용 무수정.
- passes 전환: 없음(HARNESS-golden은 여전히 false — payday fixture는 미작성). 단 기존 waterfall 골든의 CI 무결성은 복구.
- 다음: FLOW-03 항목 참조(아래). HARNESS-golden 본체(payday fixture)는 owner.
- 메모: ① **재발 방지** — `.gitattributes`가 golden/**를 eol=lf로 이미 고정하므로, 워킹트리가 LF인 한 다음 goldenLock부터는 정상. 메모리 `golden-hash-crlf-breaks-linux-ci`에 진단·교정 절차 기록. ② **교훈** — 로컬 verify 초록불 ≠ CI 초록불(줄바꿈 의존 해시). 골든/바이트 검증 파일은 워킹트리 eol을 항상 LF로 유지. ③ 커밋 33ef2f1.

## 2026-06-14  —  [FLOW-03 완료] 사이클 스냅샷 plan_line 빌더 + 골든 LIVING 검증 (owner_only 페어)
- 한 일: 직전까지 분배 *계산*(WaterfallSplit)은 끝났으나 스냅샷 미구축이라 false 유지였던 FLOW-03를, **순수 스냅샷 빌더**로 닫음(owner와 범위 결정 후 페어 구현). ① `cycle/domain/PlanLineType`(enum, ERD plan_lines.line_type 미러: ITEM/ENVELOPE/LIVING/EXTRA — FLOW-03은 ITEM·LIVING만 산출, ENVELOPE=Phase3·EXTRA=Phase6이 emit). ② `cycle/domain/PlanLineDraft`(record, plan_line 1건의 **계산 산출물** — lineType·budgetItemId·category·accountId·plannedAmount + 타입별 불변식. 이름/통장별칭 스냅샷·status는 영속화 시점 메타라 제외 = WaterfallQueryService 분담과 동형). ③ `cycle/domain/CycleSnapshotBuilder`(의존성 0 순수 — `build(income, lines, envelopeContribution, livingAccountId)`: ITEM 라인 입력순 펼침 + 구현규칙 3장 LIVING=split.living. 금액은 owner의 WaterfallCalculator/Split에 위임, 계산 0줄). ④ 단위 `CycleSnapshotBuilderTest` 13건. ⑤ 골든 `CycleSnapshotGoldenTest`(기존 waterfall-cases.json 소비 — **LIVING planned_amount=356,107 일치** = FLOW-03 verify 본체).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 단위 13 + 골든(노션 실데이터 LIVING 356,107·ITEM 순서/EMERGENCY·Σ불변식) 통과, 기존 단위·골든·ArchUnit·통합 전부 유지. **기존 fixture 필드만 소비 → goldenLock 불필요**(GoldenFixtureIntegrityTest 통과로 확인, 메모리 `golden-fixture-needs-owner-lock` 규칙 준수). `npm run verify`는 무변경(세션 시작 시 green).
- passes 전환: **FLOW-03 → true**.
- 다음: **Phase 1 위임 가능 항목 전부 소진**. 남은 건 ① **HARNESS-golden**(owner — payday fixture + goldenLock) ② **VERIFY-notion-match**(수동 — 브라우저 폭포 단계별 대조). Phase 2 관문은 owner_only **PaydayResolver + CYCLE-01/02/03**(실지급일 산출·사이클 정의·멱등 스냅샷 *영속화*) — 이번 빌더를 그대로 호출해 cycles/plan_lines에 박으면 됨. CYCLE-03가 영속화 시 PlanLineDraft.budgetItemId→항목·통장으로 name/account_name 스냅샷 채움.
- 메모: ① **범위 결정(owner 확정)** — FLOW-03 verify는 "골든 LIVING planned_amount 일치"이지 DB 영속화·payday·배치가 아니므로 **순수 빌더 + 골든**으로 정의 충족. 영속화/실지급일/멱등 생성은 CYCLE-03/01(Phase 2)로 분리(B안의 cycle_start stub은 고아 필드라 회피 — MOD-01·FLOW-02 패턴과 동류). ② **도메인 결정 3건(owner 비준)**: ⓐ EMERGENCY 항목은 `line_type=ITEM`+`category_snapshot=EMERGENCY`(plan_lines에 EMERGENCY 타입 없음, 실 이체라 일반 항목처럼) ⓑ shortfall(living<0)이면 LIVING 라인 **생략**(이체할 돈 없음 → 사용자 조정 먼저, WaterfallSplit 의미론과 일치) ⓒ LIVING 생성조건 = `livingAccountId!=null && living>0`(0원 이체도 생략). ③ **불변식**: 봉투 라인 미존재라 Σ(plan_lines)=income−envelopeContribution(골든 envelope 0 → =income 2,473,110). 봉투 도입 시 ENVELOPE 라인이 envelopeContribution만큼 채워 다시 =income. ④ **accountId 비대칭 결정** — ITEM 라인 accountId=null(영속화 시 budgetItemId→항목으로 해석), LIVING은 livingAccountId 보유(조인할 원본 없음). ⑤ **WaterfallResult.groups는 EMERGENCY 제외**라 빌더는 result가 아닌 원본 WaterfallLine 리스트를 받아 EMERGENCY 라인을 개별 산출(emergencyTotal로만 있으면 복원 불가). ⑥ Flyway 새 버전 없음(cycles/plan_lines는 V1에 이미 존재, 영속화는 Phase 2). ⑦ spotless가 신규 파일 LF/포맷 자동 정리(spotlessApply verify 한 번에).

## 2026-06-14  —  [SCR-03 완료] 홈 폭포 화면 — 남는 돈 헤더·카운트업·과배분 조정 후보
- 한 일: 직전 FLOW-02(`GET /me/waterfall`) 응답 위에 홈 화면을 얹음(빈 골격 HomeView → 실 화면). ① `api/waterfall.ts`(신규) — `getWaterfall()` + `Waterfall`/`WaterfallGroup`/`WaterfallItem`/`WaterfallSplit` 타입(백엔드 `WaterfallResponse` 1:1 미러, `Category`는 budgetItems에서 type-only import). ② `views/HomeView.vue`(재작성) — **남는 돈 헤더**(카운트업 0→remaining, easeOutCubic 600ms, `prefers-reduced-motion` 존중·rAF 없으면 즉시 최종값) + 월급 대비 비율 캡션(remaining/income, **저축률 아님**) + 비상금/생활비 분배 칩(living<0이면 빨강), **폭포 카드**(스택 바 세그먼트 + 카테고리 그룹별 차감 행[2건↑이면 건수] + 봉투 적립 행[envelopeContribution>0일 때만] + 남는 돈 total 행), **overAllocated면 경고 배너**(부족액=−living) + **유연성 순 조정 후보**(프론트가 정렬). 로딩/에러/빈 상태 분기. ③ `HomeView.spec` 6건(실 라우터+i18n, mock API). ④ i18n `home.*`(loading·incomeOf·emergency·living·envelope·count·overAllocated.banner·flex.0/1/2) ko/en. 문구 하드코딩 0(규칙 7).
- 초록불: `npm run verify` 통과 — eslint 0/0 · type-check · **vitest 62 passed**(신규 6건: 분배 표시[remaining 720,000·비상금 200,000·생활비 520,000·29% 캡션]·그룹 행/건수[그룹3+남는돈1=4행, INVESTMENT 2건·FIXED 3건 표기]·빈 상태[groups 0→waterfall 카드 없음]·과배분 배너[부족액 300,000·amount over 클래스]·**후보 정렬[living<0 제외 → 비상금(유연)→INVESTMENT(중간)→SAVING·FIXED(고정, 금액 큰 순)]**·에러) · build 성공(HomeView는 router 정적 import라 index 청크에 번들). 백엔드 무변경 — `./gradlew verify` BUILD SUCCESSFUL, 골든 fixture 무수정 통과.
- passes 전환: **SCR-03 → true**.
- 다음: **위임 가능 신규 기능 사실상 소진**. 남은 Phase 1 항목은 ① **FLOW-03**(owner_only — 사이클 스냅샷 `cycles`·`plan_lines` + LIVING plan_line 생성. 분배 계산은 FLOW-01/03에서 끝났으나 스냅샷 미구축이라 false 유지) ② **HARNESS-golden**(owner — waterfall/payday fixture + goldenLock) ③ **VERIFY-notion-match**(수동 — 노션 데이터 전체 입력 후 브라우저로 폭포 단계별 잔액 대조, 이제 SCR-03 홈이 붙어 실제 대조 가능). → **owner의 사이클 스냅샷이 다음 관문**. 에이전트 위임 후보는 owner 작업 보조뿐.
- 메모: ① **범위 한정(고아 요소 회피)** — 사이클 라벨·날짜(`6월 사이클 · 6.25~7.26`)는 사이클 스냅샷 미구축이라 응답에 없어 제외, 월급날 체크리스트(mockup SCR-03b·CYCLE-04/06 미구현) 제외, 항목 탭→MOD-01 편집(ITEM-07 PATCH 미구현, mockup 평상시 행은 카테고리 레벨이라 항목 편집 동선 불명확)도 제외 — 항목 관리는 기존 전체 탭 `/items`(MOD-01)가 담당. MOD-01/SCR-07의 "wired 목적지만 노출" 결정과 동류. ② **유연성 순위(FLOW-02 프론트 책임)** — `FLEX_RANK` LIVING·EMERGENCY=0 > INVESTMENT=1 > SAVING·FIXED·INSURANCE·SUBSCRIPTION=2. 후보는 줄일 수 있는(양수) 버킷만(living<0이면 제외 — 그게 부족분의 원인) → (flex asc, amount desc) 정렬. flex 태그 유연/중간/고정. ③ **overAllocated 의미** — = split.living<0(FLOW-02 확정). remaining 양수여도 비상금이 더 크면 과배분. 헤더 남는 돈은 remaining 그대로 표시(음수면 over 빨강), 부족액 배너는 −living. ④ **카운트업 테스트 안정** — `window.matchMedia`로 reduced-motion 감지 시 rAF 생략·즉시 최종값. 스펙에서 `vi.stubGlobal('matchMedia', () => ({matches:true}))`로 동기 렌더 보장(jsdom rAF 의존 회피). ⑤ **스택 바 색** — 카테고리별 색은 시각 정본 화면설계.html SCR-03 평상시를 따른 로컬 hex 맵(`CATEGORY_COLOR`, 문구 아니라 규칙 7 무관, 토큰에 없는 폭포 전용 셰이드). 봉투=보라, 남는 돈=회색. ⑥ **캡션 비율은 저축률 아님** — remaining/income 단순 표시 비율. savingsRate(투자 포함 토글, SET-02 Phase 5)는 응답·표시 모두 미포함 유지. ⑦ **라이브 프리뷰 미수행** — `/me/waterfall`은 실 JWT 필요(없으면 401→로그인). 이전 모든 프론트 화면과 동일하게 mock API 컴포넌트 테스트로 커버, 실 브라우저 대조는 VERIFY-notion-match 소관. ⑧ Flyway·골든 새 케이스 없음(프론트 전용). ⑨ 커밋 cd43a2b.

## 2026-06-14  —  [FLOW-02 완료] GET /me/waterfall 응답 조립 + overAllocated 과배분 판정
- 한 일: 완성된 폭포 코어(FLOW-01 `WaterfallCalculator`·FLOW-03 `WaterfallSplit`) 위에 **라이브 폭포 조회 엔드포인트**를 얹음. owner_only인 "폭포 계산"은 손대지 않고 **응답 조립만**(FLOW-02 = 조립, 위임 가능). ① `cycle/WaterfallQueryService` — 활성 항목(`findByUserIdAndStatusOrderBySortOrderAsc ACTIVE`)을 `WaterfallLine`으로 변환 → owner의 `WaterfallCalculator.calculate(baseIncome, lines, 0)` + `WaterfallSplit.from(result)` 호출(계산 로직 0줄) → budgetItemId→항목·accountId→통장별칭 맵으로 items[] 재조립. ② `cycle/WaterfallResponse`(record + 중첩 Group/Item/Split) — `income·groups·envelopeContribution·remaining·split{emergency,living}·overAllocated`. ③ `cycle/WaterfallController` `GET /api/v1/me/waterfall`(인증 필수, principal=userId). ④ `WaterfallIntegrationTest` 6건(Testcontainers, 실 JWT + POST 흐름으로 셋업).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 통합 6건(노션 실데이터 응답 조립[remaining 556,107·living 356,107·5그룹·EMERGENCY 제외·accountName·endDate/만기 null], **remaining 양수(100,000)+비상금(200,000) 과배분→living -100,000·overAllocated true**, 빈 항목→groups 0·remaining=income, EMERGENCY groups 제외·split.emergency 집계, 타인 항목 비포함, 토큰 없이 401) + 기존 단위·골든·ArchUnit·다른 통합 전부 유지. **골든 fixture(폭포·적금 만기) 무수정 통과**. `npm run verify` 통과(프론트 무변경). 노션 실데이터가 HTTP 응답으로도 1원까지 일치.
- passes 전환: **FLOW-02 → true**.
- 다음: **SCR-03 홈 폭포 화면**(이제 GET /me/waterfall 응답 위에 얹을 수 있음 — 남는 돈 헤더·폭포 리스트·카운트업, overAllocated면 경고 + 유연성 순(LIVING·EMERGENCY > INVESTMENT > SAVING·FIXED) 조정 후보 정렬은 **프론트가 수행**, 위임 가능). 또는 owner_only **사이클 스냅샷 생성**(`cycles`·`plan_lines` + LIVING plan_line → FLOW-03 true 전환). VERIFY-notion-match는 SCR-03까지 붙으면 브라우저 수동 대조.
- 메모: ① **overAllocated 의미 확정(중요)** — `= split.living < 0`. 요구사항 "배분 합계가 수입 초과"는 비상금(EMERGENCY)까지 포함한 배분이 income을 넘는 상황이고 그게 곧 생활비 음수 → remaining 양수여도 비상금이 더 크면 과배분(remaining<0은 부분집합). 직전 FLOW-03 owner 메모("overAllocated가 shortfall 신호를 이어받아야")와 일치. **API명세 line 49 초안의 `remaining<0`은 부정확이라 정정**(구현규칙 "코드와 충돌 시 문서 수정"). ② **owner_only 비침범** — 클래스명을 `WaterfallService`(작업 분담의 owner_only "폭포 계산")가 아니라 `WaterfallQueryService`로 두고 산술은 전부 owner 도메인에 위임(이 서비스엔 계산 0줄). ③ **범위 보류 3건**(고아 필드/조기구현 회피, MOD-01 패턴) — `envelopeContribution`=0(봉투 Phase 3 미구현, 실제 현재값), `items[].expectedMaturityAmount`=null(ITEM-05 Phase 5), **`savingsRate` 응답 제외**(SET-02 Phase 5가 "저축률 산정 방식 폭포 표시 공통 적용" 소유 — 그 Phase에서 추가). ④ **스냅샷과 독립** — `/me/waterfall`은 사이클 스냅샷이 아니라 base_income·활성 항목 라이브 계산. `/cycles`(스냅샷)와 별개라 owner의 스냅샷 작업 없이 착수 가능했음. ⑤ **계정명 조회** — `accountRepository.findAllById(distinct accountIds)`로 일괄(별도 쿼리 메서드 안 만듦). 항목의 accountId는 생성 시 소유권 검증을 거쳐 호출자 소유가 보장되므로 userId 재검증 불필요. 비활성화된 통장은 조회 안 돼 accountName=null(허용). ⑥ ArchUnit: `WaterfallQueryService`는 `..domain..`이 아니라 cross-도메인 infra 의존 OK, double/float 없음(savingsRate 제외로 비율 계산도 없음). ⑦ Flyway 새 버전 없음(읽기 전용 조립). 골든 새 케이스 없음(통합 테스트는 골든 파일과 독립 입력으로 같은 숫자 교차 확인만). ⑧ 커밋 efb327b. **무관 변경**: 이번 세션 중 `.claude/commands/wrap.md`에 rename 제안 단계가 추가돼 있어 FLOW-02 커밋에서 제외, 별도 doc(harness) 커밋으로 분리.

## 2026-06-14  —  [FLOW-01 완료 + FLOW-03 분배 계산] 폭포 캐스케이드·남는 돈 분배 순수 계산기 (owner_only 페어)
- 한 일: owner_only 폭포 계산을 **페어 모드**로(도메인 결정은 전부 owner가, 골든 lock도 owner가). ① `cycle/domain` 신설 — `WaterfallLine`(budgetItemId·category·amount 검증)·`WaterfallGroup`·`WaterfallResult`·`WaterfallCalculator`(의존성 0 순수). 캐스케이드: 카테고리 그룹 소계 → `remaining=income−Σ소계−envelopeContribution`(음수 clamp 금지), **EMERGENCY는 groups 제외하고 emergencyTotal로만 집계**(나머지의 분배). Math.addExact/subtractExact 오버플로 차단. owner 결정: 별도 `GROUP_DISPLAY_ORDER` 상수(SAVING→INVESTMENT→FIXED→INSURANCE→SUBSCRIPTION)·emergencyTotal 노출·빈 group 생략·budgetItemId 포함. ② `WaterfallSplit`(FLOW-03 분배): emergency=emergencyTotal·living=remaining−emergencyTotal·shortfall=living<0, split 합=remaining 불변식 강제. **owner 도메인 결정: 과배분(living<0)이면 자동 분배 안 하고 shortfall=true로 사용자 판단에 넘김**(비상금 줄일지·생활비 줄일지·비율 유지는 사용자 몫 — FLOW-02 조정 후보가 이어받음). ③ 골든 `waterfall-cases.json`(owner 작성) + `WaterfallGoldenTest`(golden 패키지, Jackson) — 노션 실데이터 검증: 실수령 2,473,110 → 단계별 잔액 973,110·662,510·566,857·556,107, 생활비 356,107, shortfall false. owner `goldenLock` 2회로 잠금. ④ 단위 테스트: `WaterfallCalculatorTest` 10건 + `WaterfallSplitTest` 8건.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL(기존 골든 무수정 통과 + 신규 waterfall 골든 케이스는 owner goldenLock으로 golden.sha256에 등록 — maturity+waterfall 2파일), `npm run verify` 통과(프론트 미변경). **노션 실데이터 1원까지 수동 대조 일치**.
- passes 전환: **FLOW-01 → true**(골든 폭포 fixture 통과 + 단위 테스트 충족). **FLOW-03는 false 유지**(ⓑ 결정 — 분배 계산은 끝났으나 users.living_account_id로 LIVING plan_line 실제 생성은 사이클 스냅샷 미구축 단계 소관).
- 다음: **사이클 스냅샷 생성(`cycles`·`plan_lines` 엔티티 + LIVING plan_line 생성, 구현규칙 3장 PENDING 갱신 규칙)** — owner_only, 페어. 그 위에 **FLOW-02**(GET /me/waterfall 응답: groups·split·overAllocated·shortfall 조립, 위임 가능) → **SCR-03**(홈 폭포 화면, FLOW-02 응답 의존). 계산 코어(FLOW-01/03)는 완료라 다음 관문은 owner의 스냅샷 설계.
- 메모: ① **owner_only 페어 운영** — FLOW-01/03 본체를 owner 결정(표시 순서·EMERGENCY 위치·과배분 처리)을 물어가며 구현, goldenLock은 agent deny라 owner가 직접 2회 실행. 규칙 주인이 owner라 본인이 풀 수도 있으나 이번엔 페어 선택. ② **과배분 결정(중요)** — living<0이면 자동 분배·clamp 안 하고 사용자에게 넘김. FLOW-02 overAllocated가 이 신호를 이어받아야 함(메모리 `waterfall-notion-golden-data`에 기록). ③ **골든 케이스 추가** — `waterfall-cases.json`(노션 실데이터). fixture 수정 시마다 owner `goldenLock` 재실행 → golden.sha256 갱신 전까진 verify 빨간불(`GoldenFixtureIntegrityTest`). lock 시 owner 확정값: 보험 95,653(94,984 아님)·envelopeContribution 0·일투자 제외. ④ **cmd 함정** — gradlew 명령에 `#` 주석 붙이면 cmd가 별도 task로 파싱해 실패(`Task '#' not found`). 한글 콘솔 출력 mojibake는 CP949 탓(실행 무관). ⑤ Flyway 새 버전 없음(계산 도메인은 DB 무관, plan_lines는 스냅샷 단계에서). ⑥ 커밋 3건: 245042c(계산기)·cef1d14(골든)·79f31dd(분배).

## 2026-06-14  —  [HARNESS-claude 완료] .claude 훅·커맨드·권한 실발동 검증 + 포맷 훅 수정
- 한 일: Phase 0 마지막 잔여 하네스 항목. `.claude/` 설정을 **실제로 발동시켜** 검증하고, 그 과정에서 발견한 두 결함을 수정. ① **PreToolUse 마이그레이션 보호** — 실 Edit 도구로 기존 `V1__init.sql` 수정 시도 → 라이브 차단(exit 2, 파일 원본 유지) 확인. stdin JSON 계약으로 단위 검증(기존 V1 차단 / 신규 V2 허용 / 일반 파일 허용 / 백슬래시 경로 차단). ② **PostToolUse 포맷 훅 결함 수정** — `format.ps1`이 `prettier --write`를 부르나 **prettier가 이 프로젝트 의존성이 아님**(devDeps는 oxlint+eslint뿐) → 항상 "missing packages"로 무동작이었음. 프로젝트 실제 fixer `oxlint --fix → eslint --fix`(=`npm run lint:fix`, CLAUDE.md 실행 명령 사전)로 교체. eslint flat config가 cwd 기준이라 `frontend/`에서 단일 파일만 처리. 임시 .ts로 `let`→`const` 자동수정 발동 확인. ③ **훅 UTF-8 BOM 결함 수정** — 두 훅이 BOM 없는 UTF-8이라 PS5.1이 시스템 ANSI(CP949)로 파싱 → 한글 차단 메시지 mojibake. UTF-8 BOM 추가로 정상 출력 복구(`차단: 기존 Flyway 마이그레이션(...)은 수정 금지...`). ④ **/task 동작** — 본 세션이 `/task HARNESS-claude`로 구동된 것 자체가 입증(bearings·wrap 포함 `.claude/commands/*.md` 존재). ⑤ **권한** — settings.json allow(gradlew/npm/docker)·deny(force push·flyway clean·goldenLock·`down -v`·`rm -rf`) 선언 확인. CLAUDE.md 실행 명령 사전도 이미 실 명령으로 채워져 있음(기존).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL, `npm run verify`(lint·type-check·vitest·build) 통과 — 변경은 `.claude/hooks/*.ps1` 2개뿐이라 백엔드/프론트 소스 무변경, 골든 fixture 무수정. 훅 실발동: protect-migrations 라이브 차단(exit 2)·단위 4케이스, format 훅 let→const 자동수정.
- passes 전환: **HARNESS-claude → true**. (Phase 0 잔여는 이제 HARNESS-golden뿐 — waterfall/payday fixture + goldenLock 소유자 전용.)
- 다음: **위임 가능 신규 기능 사실상 소진**. Phase 1 남은 항목은 전부 폭포 계열에 막힘 — FLOW-01/03 **owner_only**(WaterfallService·비상금/생활비 분배), FLOW-02는 그 의존, SCR-03 홈 폭포는 FLOW-02 응답 의존, VERIFY-notion-match는 폭포 완성 후 수동. HARNESS-golden도 owner(기대값 실데이터·goldenLock). → **owner의 폭포 작업이 다음 관문**. 에이전트 위임 후보는 FLOW 계열 설계 보조(순수 계산 클래스 골격·테스트 케이스 제안만, 통째 구현 금지)뿐.
- 메모: ① **prettier 부재 결정** — 이 프로젝트는 의도적으로 prettier 없이 oxlint+eslint만 쓴다. eslint 설정에 공백/스타일 규칙이 없어 `eslint --fix`는 실 lint 위반(prefer-const 등)만 고치고 공백 재정렬은 안 함 — 그게 프로젝트의 "포맷 적용" 의미(`npm run lint:fix`). 포맷 훅을 이 도구에 맞춤(prettier 추가는 owner 결정 없이 스코프 침범이라 안 함). ② **PS5.1 BOM 함정** — `powershell.exe`(5.1)는 BOM 없는 .ps1을 CP949로 파싱해 한글 리터럴이 파싱 시점에 깨진다(출력 인코딩 utf-8과 무관). 한글 포함 .ps1은 UTF-8 BOM 필수. Write 도구는 BOM 없이 저장하므로 직후 BOM 재주입 필요. 메모리 `ps1-hooks-need-utf8-bom`에 기록. ③ 라이브 훅 변경은 세션 시작 시 로드라 이번 세션의 protect/format 라이브 동작은 변경 **전** 버전이었으나(차단은 작동, 메시지만 깨짐), 스크립트 직접 실행으로 수정본 검증 완료 — 다음 세션부터 라이브 반영. ④ Flyway·골든 새 케이스 없음(하네스 전용). ⑤ 커밋 함정 재발: PowerShell here-string에 큰따옴표 섞이면 -m 파싱 깨짐 → `git commit -F <파일>`로 우회.

## 2026-06-14  —  [SCR-02 완료] 온보딩 스텝1 — 실수령액·월급일·조정 규칙 입력
- 한 일: 직전 SET-01(GET/PATCH `/me`)의 짝 프론트. AUTH-01 로그인 직후 첫 가입 분기. ① `api/me.ts`(신규) — `getMe`/`updateMe` 래퍼 + `Me`/`MeUpdate` 타입 + `PAYDAY_ADJUSTMENTS` 상수(서버 enum 미러: PREV_BUSINESS_DAY/NEXT_BUSINESS_DAY/NONE). client baseURL이 `/api/v1`이라 경로는 `/me`. ② `views/OnboardingView.vue`(신규, **스텝1만**) — 실수령액(천 단위 표시, 제출 시 정수 파싱·ItemFormSheet 패턴), 월급일(프리셋 칩 10·21·25·말일(31) + `직접 입력` 칩→number 인풋), 조정 규칙 칩 3종. **서버 상한 미러 클라 검증**(실수령액 1~10억·payday 1~31 → 위반 시 VALIDATION_FAILED·서버 미호출), 서버 `ApiError.code`→`errors.{code}`. mockup SCR-02 스텝1/3 dots 표시. ③ `/onboarding` 라우트(name `onboarding`, 인증 필수·chrome:false). ④ `AuthCallbackView` — `loginWithCode` 반환 세션의 `isNewUser`면 `{name:'onboarding'}`, 아니면 `home`으로 분기(기존 "항상 home" 대체). ⑤ i18n `onboarding.*`(step1 라벨·adjustment 3라벨·dayN 보간) ko/en. 문구 하드코딩 0(규칙 7).
- 초록불: `npm run verify` 통과 — eslint 0/0 · type-check · **vitest 55/55**(신규 9건: `OnboardingView.spec` 8건[신규 사용자 실수령액 빈칸·기존 설정 prefill+25일 칩 on·입력→다음→updateMe(천단위 파싱)→홈·생활비통장 지정 보존·실수령액 빈값 VALIDATION_FAILED·월급일 미선택 VALIDATION_FAILED·직접입력 32 경계 VALIDATION_FAILED·조정 규칙 NONE 선택 payload 반영], `AuthCallbackView.spec`에 isNewUser→온보딩 분기 1건) · build 성공(OnboardingView 청크 분리). 백엔드 무변경 — 세션 시작·종료 시 `./gradlew verify` BUILD SUCCESSFUL, 골든 fixture 무수정.
- passes 전환: **SCR-02 → true**.
- 다음: **위임 가능 항목 거의 소진**. Phase 1 백엔드는 폭포 계열만 — FLOW-01/03 **owner_only**, FLOW-02는 그에 의존, SCR-03 홈 폭포는 FLOW-02 응답 의존. → 남은 후보 ① **HARNESS-claude**(Phase 0 잔여: 훅 발동·/task 동작 검증, 위임 가능) ② **FLOW 계열 설계 보조**(순수 계산 클래스 골격·테스트 케이스 제안만, 통째 구현 금지). VERIFY-notion-match는 폭포 완성 후 수동. 사실상 폭포(owner) 진행 없이는 신규 프론트 화면(SCR-03)·FLOW-02가 막혀 owner 작업이 다음 관문.
- 메모: ① **스텝1 한정 결정** — feature_list가 SCR-02를 "온보딩 스텝1"로 못박음. mockup은 3스텝(스텝2 항목·통장=MOD-01/03 이미 별도 화면 완료, 스텝3 폭포 미리보기=FLOW-02 의존 미구현)이라 스텝1 저장 후 곧장 홈으로. 온보딩 플로우 봉합(스텝2/3 연결)은 FLOW-02·폭포 화면 도입 시. ② **신규 사용자 빈 폼 결정** — AUTH-01 플레이스홀더(baseIncome=0·payday=1·NONE)는 임의값이라 prefill하면 "1일·그대로" 오선택 유도 → `baseIncome>0`(=등록 이력)일 때만 월급일·조정 규칙 prefill, 신규는 빈 폼에서 직접 고르게. (payday=1 placeholder를 prefill했더니 "월급일 미선택" 테스트가 깨져 발견.) ③ **livingAccountId 보존** — PATCH /me는 전체 갱신(SET-01 결정)이라 step1이 null로 덮으면 step2 지정이 날아감 → GET /me의 livingAccountId를 폼에 보관 후 그대로 되돌려 보냄. ④ **함정 재발: vi.mock 풀 automock이 상수 export를 깸** — `vi.mock('@/api/me')`가 `PAYDAY_ADJUSTMENTS`를 undefined로 만들어 조정 규칙 칩 v-for가 안 그려짐(ItemFormSheet의 CATEGORIES와 동일) → `importOriginal` 부분 모킹으로 상수 유지·`getMe`/`updateMe`만 vi.fn. 이 패턴은 이제 2회째 — **상수+함수 혼합 export 모듈은 항상 부분 모킹**이 규칙. ⑤ 라우트 가드는 마운트 시 발화 안 함(navigation 시점) — 테스트는 `router.replace` spy로 의도만 검증(AuthCallback 패턴). ⑥ Flyway·골든 새 케이스 없음(프론트 전용). ⑦ **라이브 프리뷰 미수행** — `/onboarding`은 실 JWT로 GET /me를 쳐야 도달(없으면 /login), OAuth 실 client ID 필요 → mock API 컴포넌트+실 라우터 테스트로 커버(MOD-01/03·SCR-01과 동일).

## 2026-06-14  —  [SET-01 완료] 기본 정보 등록 PATCH /me + 프로필 조회 GET /me
- 한 일: 온보딩(SCR-02)의 짝 백엔드. `user` 패키지에 설정 엔드포인트 신설. ① `user/domain/PaydayAdjustment` — 지급일 조정 규칙 순수 enum(PREV_BUSINESS_DAY/NEXT_BUSINESS_DAY/NONE, 규칙 9 프레임워크 비의존). ② `User` 엔티티 연장 — `living_account_id` 매핑 추가(nullable, FK는 plain Long 관례), `paydayAdjustment`를 String→enum `@Enumerated(STRING)` 전환(컬럼 varchar 유지, 기존 "NONE" 행 호환), `updateSettings(baseIncome, payday, paydayAdjustment, livingAccountId)` 뮤테이터 + SET-01 필드 getter들. ③ `UserService` — `getMe`/`updateSettings`. **생활비 통장 지정 시 통장 소유권 관문 재사용**(`AccountRepository.findByIdAndUserIdAndActiveTrue`) → 미소유·삭제·부재는 전부 NOT_FOUND(존재 비노출, 통장 패턴 미러). ④ `UserController` — `/api/v1/me` GET(프로필+설정)·PATCH. baseIncome 1~10억·payday 1~31 bean validation, paydayAdjustment @NotNull. ⑤ `feature_list.json`에 **누락돼 있던 SET-01 엔트리 추가**(요구사항정의서엔 있으나 트래커에 없었음).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규 `UserIntegrationTest` 11건(Testcontainers postgres:16-alpine): 신규 플레이스홀더 GET(baseIncome 0·payday 1·NONE·ko·livingAccountId null), **등록 반영+재조회 일치**, 본인 활성 통장 생활비 지정, **타인 통장 지정 차단(NOT_FOUND, 설정 불변)**, 삭제된 통장 지정 차단, **지정 후 null 해제**, payday 0/32 VALIDATION_FAILED, baseIncome 0/10억초과 VALIDATION_FAILED, 미지원 enum값 VALIDATION_FAILED, 필수 누락 VALIDATION_FAILED, 토큰 없이 401. 기존 단위·골든·ArchUnit·AccountIntegrationTest·AuthIntegrationTest 전부 유지(골든 무수정 통과). `npm run verify` 47/47 통과(프론트 무변경).
- passes 전환: **SET-01 → true**(엔트리 신규 추가).
- 다음: 짝 프론트 **SCR-02 온보딩**(이제 GET/PATCH /me 위에 얹음 — `auth.isNewUser` 분기 → 실수령액·월급일·조정규칙 입력, payday 1~31 E2E). 그 외 위임 가능 Phase 1 기능은 전부 owner(FLOW-01/03)·그 의존(FLOW-02)·FLOW 의존(SCR-03)에 막혀 있어 SCR-02가 자연스러운 후속. HARNESS-claude(Phase 0, 훅·/task 검증)도 잔여 후보.
- 메모: ① **PATCH 전체 갱신 결정** — partial PATCH의 livingAccountId null 모호성(미변경 vs 해제)을 피하려 baseIncome·payday·paydayAdjustment 필수 + livingAccountId nullable의 **전체 설정 갱신**으로 설계. 폼이 GET /me로 현재값 채운 뒤 통째 제출하는 표준 패턴. livingAccountId=null은 명시적 해제. ② **SET-02(투자 토글)·SET-03(locale) 쓰기 제외** — 각 Phase 5/7+ 소관. 컬럼은 V1에 이미 있고 default(true/'ko')라 GET /me 응답엔 현재값 노출만, PATCH는 SET-01 4필드만 씀(MOD-01의 조건부 블록 제외와 동류 — 고아 필드 회피). ③ **API명세 PATCH /me 본문 확장** — 명세 v1.0(ERD v1.1/요구사항 v1.3 이전)엔 livingAccountId가 없으나, SET-01 v1.3이 생활비 통장 지정을 포함하므로 추가. 명세는 "구현 후 Swagger 정본". ④ Flyway 새 버전 없음 — users 11컬럼(living_account_id 포함)은 V1에 이미 존재, @Enumerated STRING은 기존 varchar에 검증 통과. ⑤ 골든 새 케이스 없음. ⑥ 함정: Write가 LF로 써서 spotless 빨간불 가능 → `spotlessApply verify` 한 번에 실행(기존 sed-crlf 메모와 동류). gradle은 JAVA_HOME=jdk-22 우회 여전. ⑦ payday DTO는 Integer로 받아 service에 `(short)(int)`로 전달(smallint 컬럼).

## 2026-06-14  —  [SCR-07 완료] 전체 탭 골격 — 허브 화면 + 하단 탭 배선
- 한 일: 직전 세션이 명시한 후속 — `/accounts`(MOD-03)·`/items`(MOD-01) 라우트는 이미 있었고 진입 동선만 연결. ① `views/MenuView.vue` — **전체 허브(SCR-07, /menu)**. `통장 관리`→/accounts·`배분 항목 관리`→/items 진입 행(카드 메뉴, 셰브론) + `로그아웃`(auth.logout → /login). **API 호출 없는 순수 정적 화면**(accounts/items와 달리 onMounted fetch 없음). ② `router/index.ts` — `/menu`(name `menu`) 라우트 추가, 기존 두 라우트 주석을 "전체 허브에서 진입"으로 확정. ③ `App.vue` — 하단 탭 `전체`→`/menu` 배선(onSelect에 `all` 분기), **라우트→탭 키 매핑 TAB_BY_ROUTE**(menu·accounts·items → `전체` 탭 활성 유지, 그 외 home). checklist(봉투·SCR-04)·report(SCR-06) 탭은 화면 도입(Phase 3/5) 시 연결로 주석 명시. ④ i18n `menu.*`(title·accounts·items·logout) ko/en. 문구 하드코딩 0(규칙 7).
- 초록불: `npm run verify` 통과 — eslint 0/0 · type-check · **vitest 47/47**(신규 8건: `MenuView.spec` 4건[관리 행+로그아웃 표시·통장행→/accounts·항목행→/items·로그아웃→세션 비우고 /login] + `TabNav.spec` 4건[홈→전체 탭→/menu·허브→홈 탭→/·허브에서 전체 탭 on 표시·통장 하위화면에서도 전체 탭 on 유지], **실 라우터+App 셸 E2E**) · build 성공(MenuView 청크 분리 확인). 백엔드 무변경 — 세션 시작 시 `./gradlew verify` BUILD SUCCESSFUL 확인, 골든 fixture 무수정.
- passes 전환: **SCR-07 → true**.
- 다음: **자연스러운 위임 항목이 거의 소진**. Phase 1 백엔드는 폭포 계열만 남음 — FLOW-01/03은 **owner_only**, FLOW-02는 그에 의존. SCR-02 온보딩은 users PATCH(**SET-01 미구현**) 의존, SCR-03 홈 폭포는 FLOW-02 응답 의존. → 후보는 ① **SET-01**(users 설정 PATCH: 실수령액·월급일·조정규칙·생활비통장 — 위임 가능, SCR-02 온보딩의 짝 백엔드) ② **FLOW 계열 설계 보조**(순수 계산 클래스 골격·테스트 케이스 제안만, 통째 구현 금지). VERIFY-notion-match는 폭포 전체 완성 후 수동.
- 메모: ① **고아 링크 금지 결정** — 보관함(SCR-08·P4)·가져오기/내보내기(DATA-01/02·P4+)·설정 상세(투자포함 SET-02·P5 / 월급일·생활비통장 SET-01 onboarding·users PATCH 미구현 / 언어 SET-03·P7)·탈퇴(회원탈퇴 백엔드 미구현)는 목적지가 없어 **미연결**. mockup SCR-07엔 이 행들이 있지만 골격 단계에선 wired 목적지만 노출(MOD-01의 ITEM-07 보류와 동류). 라우트 생기면 MenuView `links`에 추가만. ② **라이브 프리뷰 미수행** — `/menu`는 가드상 실 JWT 필요(없으면 /login). MenuView 자체는 API 없는 정적 화면이나 도달에 인증 필요 → MOD-01/03과 동일하게 실 라우터 기반 컴포넌트/E2E 테스트로 커버. ③ **탭 활성 매핑** — current를 `route.name`에서 TAB_BY_ROUTE로 변경(하위 화면 accounts/items도 전체 탭 on 유지). ④ **테스트 함정**: 메뉴 행 버튼 텍스트에 셰브론(`›`)이 붙어 `.text()===` 정확 매칭 실패 → `includes`로. 탭 클릭→lazy 라우트(MenuView) 로딩 완료를 보장하려면 `flushPromises`만으론 부족 → push를 spy로 두고 **반환된 내비게이션 promise를 await**(원 동작 유지). `results.at(-1)`은 vue-tsc 타깃 lib에 `Array.at` 없어 TS2550 → 인덱스 접근으로. ⑤ Flyway·골든 새 케이스 없음(프론트 전용). ⑥ BottomNav 2번 탭 키는 `checklist`(라벨 체크리스트)인데 mockup은 봉투 — 기존 불일치, SCR-07 범위 밖이라 미수정.

## 2026-06-14  —  [MOD-01 완료] 항목 폼 v1 — 공통 필드 생성 + 목록 + soft delete
- 한 일: 짝 백엔드 ITEM-01(생성·조회)·ITEM-09(soft delete) 위에 항목 폼을 끝까지 배선. **공통 필드만 다루는 v1**(feature_list 스코프). ① `api/budgetItems.ts` — 타입드 `listBudgetItems/createBudgetItem/deleteBudgetItem` 래퍼 + `CATEGORIES` 상수(서버 `Category` enum 미러, **LIVING 제외** — 생활비는 폭포 나머지 계산값). `BudgetItem` 응답 타입은 endDate/memo 포함하되 **v1 폼은 둘 다 안 보내 서버에서 null 생성**. ② `components/ItemFormSheet.vue` — **추가 모드**: 카테고리 칩(6종)·이름·월 금액(천 단위 표시, 제출 시 정수 파싱)·대상 통장 `<select>`·시작일(기본 오늘). **서버 상한 미러 클라 검증**(이름 1~50·금액 1~10억·통장 필수·시작일 필수 → 위반 시 VALIDATION_FAILED·서버 미호출), 서버 `ApiError.code`→`errors.{code}` i18n. **관리 모드**(기존 항목 행 탭): 수정 입력란 없이 요약(분류·금액·통장·시작일) + **2단 삭제 확인**(ITEM-09 soft delete). **통장 0개면 안내 + 저장 버튼 차단**. ③ `views/ItemsView.vue` — 항목 목록(행 탭→관리) + 폼 진입점, **통장 목록 병행 로드**(`Promise.all`)해 대상 통장명 표시·폼에 전달. 로딩·빈·에러 상태 분기. ④ `/items` 라우트(SCR-07 전체 탭 진입 예정 — 지금은 라우트만). ⑤ i18n `items.*`(카테고리 6라벨 포함) ko/en + `errors.ITEM_LIMIT_EXCEEDED`(100개 상한 409). 문구 하드코딩 0(규칙 7).
- 초록불: `npm run verify` 통과 — eslint 0/0 · type-check · **vitest 39/39**(신규 `ItemsView.spec` 9건: 마운트 목록+통장명 표시·빈 상태·**생성→폼→저장→목록 반영 E2E**·카테고리 칩 선택 반영·이름 빈 값 VALIDATION_FAILED(서버 미호출)·통장 미선택 VALIDATION_FAILED·통장 0개 저장 차단·행 탭 관리 모드 2단 삭제→deleteBudgetItem·상한 초과 ITEM_LIMIT_EXCEEDED 표시) · build 성공. 백엔드 무변경 — 세션 시작 시 `./gradlew verify` BUILD SUCCESSFUL 확인, 골든 fixture 무수정.
- passes 전환: **MOD-01 → true**.
- 다음: **SCR-07 전체 탭 골격**(통장·항목 목록·설정 진입 — `/accounts`·`/items` 라우트가 이미 있어 진입 동선만 연결하면 됨, 자연스러운 후속) 또는 온보딩 **SCR-02**(단 users PATCH=SET-01 영역 미구현). 홈 폭포 **SCR-03**은 FLOW-02 응답 의존, FLOW 계열은 owner_only(FLOW-01/03)·그 의존(FLOW-02)이라 위임 부적합. Phase 1 백엔드는 폭포 계열(FLOW-01/02/03)만 남음.
- 메모: ① **수정(ITEM-07) v1 제외 결정** — 백엔드에 항목 PATCH 엔드포인트가 없다(ITEM-07=Phase 2 미구현). 그래서 MOD-03(통장)과 달리 행 탭은 수정이 아니라 **관리(요약+삭제) 모드**로 연다. 수정 도입은 ITEM-07 백엔드와 함께. ② **저축 조건부 블록 제외** — 만기일(ITEM-02)·이율·세금·예상금액(ITEM-05/06)은 mockup상 "저축 선택 시" 묶음이고 feature_list가 "공통 필드만 — Phase 5"로 못박음. 백엔드 create는 endDate를 받지만(ITEM-02) 한 필드만 노출하면 고아 필드가 되어 블록 전체를 Phase 5로 미룸. 외화(ITEM-04)도 Phase 3. ③ **브라우저 E2E 미수행** — `/items`는 실 JWT가 있어야 API를 칠 수 있어(없으면 401→로그인) 라이브 프리뷰로 증명 불가. MOD-03과 동일하게 mock API 컴포넌트 테스트로 커버. ④ **vi.mock 부분 모킹** — `@/api/budgetItems`를 full automock하면 `CATEGORIES` 상수가 깨져 ItemFormSheet 칩이 안 그려짐 → `importOriginal`로 상수·타입 유지, 세 함수만 `vi.fn<typeof actual.fn>()`로 스텁(lint `require-mock-type-parameters` 충족). Teleport는 MOD-03처럼 `stubs.teleport=true`로 인라인 렌더, 버튼은 `buttonByText` 헬퍼로 식별. ⑤ `<select>` v-model은 숫자 `accountId`에 바인딩 — 옵션 `:value="a.id"`(number)라 `setValue('1')`이 number 1로 해석됨. ⑥ Flyway·골든 새 케이스 없음(프론트 전용). ⑦ 진입 동선(전체 탭 메뉴)은 SCR-07 소관이라 의도적 미연결.

## 2026-06-13  —  [MOD-03 완료] 통장 관리 화면 + 추가·수정·삭제 폼 (첫 Phase 1 프론트)
- 한 일: 짝 백엔드 SET-04(`GET|POST|PATCH|DELETE /accounts`) 위에 프론트 통장 폼을 끝까지 배선. ① `api/accounts.ts` — 타입드 CRUD 래퍼(`listAccounts/createAccount/updateAccount/deleteAccount`). `Account` 타입은 **id·name·purpose·bankDeepLink·sortOrder만** — 계좌번호류 필드 없음(규칙 6). purpose/bankDeepLink는 미입력 시 `null`로 송신. ② `components/AccountFormSheet.vue` — **MOD-03 폼**(BottomSheet 재사용). 별칭(name)·용도(purpose)·은행 앱 링크(bankDeepLink) 입력, **서버 상한 미러링 클라 검증**(name 1~50·purpose≤100·link≤500, `maxlength`+trim 길이 체크 → 위반 시 VALIDATION_FAILED), 서버 `ApiError.code`→`errors.{code}` i18n 표시. `account` prop null=추가/있으면 수정, `open` watch(immediate)로 열릴 때 폼 초기화. **수정 모드 인라인 삭제 확인**(삭제 클릭→confirm 단계 전환→재클릭 시 deleteAccount, soft delete). 성공 시 `saved` emit. ③ `views/AccountsView.vue` — 통장 관리 목록(활성만, 행 탭→수정) + 폼 진입점, `saved` 받으면 시트 닫고 목록 재조회. 로딩·빈 상태·에러 상태 분기. ④ `/accounts` 라우트(SCR-07 전체 탭에서 진입 예정 — 지금은 라우트만). ⑤ i18n `accounts.*` ko/en + `errors.ACCOUNT_LIMIT_EXCEEDED`(20개 상한 409). 문구 하드코딩 0(규칙 7).
- 초록불: `npm run verify` 통과 — eslint 0/0 · type-check · **vitest 30/30**(신규 `AccountsView.spec` 7건: 마운트 목록 표시·빈 상태·**생성→폼→저장→목록 반영 E2E**·이름 빈 값 VALIDATION_FAILED(서버 미호출)·행 탭 수정값 채움·updateAccount·삭제 2단 확인 후 deleteAccount·상한 초과 ACCOUNT_LIMIT_EXCEEDED 표시) · build 성공. 백엔드 무변경 — 세션 시작 시 `./gradlew verify` BUILD SUCCESSFUL 확인, 골든 fixture 무수정.
- passes 전환: **MOD-03 → true**.
- 다음: **MOD-01 항목 폼**(ITEM-01/02/09 백엔드 완비, 대상 통장 선택에 이 통장 목록 재사용 — 자연스러운 후속) 또는 온보딩 **SCR-02**(단 users PATCH=SET-01 영역 미구현). 홈 폭포 **SCR-03**은 FLOW-02 응답 의존, FLOW 계열은 owner_only(FLOW-01/03)·그 의존(FLOW-02)이라 위임 부적합.
- 메모: ① **브라우저 E2E 미수행 결정** — `/accounts`는 실 JWT가 있어야 accounts API를 칠 수 있고(없으면 401→로그인 리다이렉트), OAuth 실 client ID 필요라 라이브 프리뷰로는 기능 증명 불가. 대신 생성→목록→수정→삭제 흐름을 mock API 컴포넌트 테스트로 커버(SCR-01의 확립된 방식과 동일). ② **Teleport 테스트 패턴** — BottomSheet가 `Teleport to="body"`라 시트 내부 요소를 `wrapper.find`로 못 찾음 → `global.stubs.teleport=true`로 인라인 렌더해 해결. 폼/목록에 `.btn`이 여럿이라 ko 버튼 텍스트로 식별(`buttonByText` 헬퍼). ③ **클라 검증은 서버 규칙의 미러**(NAME_MAX=50 등 상수 복제) — 진실원천은 서버, 클라는 UX 선차단일 뿐. 상한·소유권 최종 판정은 서버. ④ 삭제는 2단 확인(window.confirm 대신 컴포넌트 상태)으로 테스트 가능하게. ⑤ Flyway·골든 새 케이스 없음(프론트 전용). ⑥ 진입 동선(전체 탭 메뉴)은 SCR-07 소관이라 의도적 미연결.

## 2026-06-13  —  [ITEM-02 완료] 만기일 등록 + 만기 경과 항목 ARCHIVED 배치
- 한 일: budgetitem 패키지 연장. ① **만기일 등록** — `BudgetItem`에 `end_date` 매핑(NULL=기한 없는 항목), `create` 팩토리·생성 서비스·컨트롤러에 `endDate` 선택 입력 추가. `CreateRequest`에 `@AssertTrue isEndDateAfterStartDate()`로 **end_date > start_date 교차 검증**(구현규칙 5장, 위반 시 400 VALIDATION_FAILED — 핸들러가 코드만 반환해 문구 누출 없음). 응답에 `endDate` 포함. ② **만기 판정 순수 정책** — `BudgetItem.isMaturedAsOf(today)` = `endDate != null && endDate.isBefore(today)`. **아키텍처 4장 "end_date 경과"를 strict before로 해석**(만기 당일은 미보관, 다음날부터 대상). 기준일은 주입 KST `Clock`으로 산출(규칙 3). ③ **멱등 배치** — `MaturityArchiveService`(application, `@Transactional`): `findByStatusAndEndDateNotNull(ACTIVE)` 조회 → `isMaturedAsOf`로 거른 뒤 `markArchived()`, 전환 건수 반환. ACTIVE만 전환하므로 **재실행 시 이미 ARCHIVED라 재매칭 안 됨**(규칙 8). DB는 status+notnull 거친 거름만, 만기 판정은 Java/KST에 둠. ④ `MaturityArchiveScheduler`(`@Scheduled cron "0 0 4 * * *" zone=Asia/Seoul`, 얇은 트리거, ADR-04 단일 인스턴스) + `common/SchedulingConfig`(`@EnableScheduling`). ⑤ **범위 외 의도적 제외**: REBALANCE_MATURITY 리밸런싱 제안(SUG-01·Phase 6 + 제안 룰=소유자), 항목 PATCH 수정(ITEM-07).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — 신규: `BudgetItemMaturityTest` 단위 4건(경계: 어제 보관/당일 미보관/미래/무만기), `MaturityArchiveBatchIntegrationTest` 통합 4건(Testcontainers + **고정 Clock 2026-06-13 KST** @TestConfiguration @Primary: 경과 ACTIVE만 전환·**재실행 0건 멱등**·활성 목록 제외·ARCHIVED/DELETED 불간섭), `BudgetItemIntegrationTest`에 HTTP 2건 추가(만기일 저장·응답 포함 / end_date ≤ start_date VALIDATION_FAILED). 골든 fixture 무수정 통과, 단위·ArchUnit·checkstyle·spotless 전부 유지. `npm run verify` 통과(프론트 미변경, 회귀 없음).
- passes 전환: **ITEM-02 → true**.
- 다음: 짝 프론트 **MOD-01 항목 폼**(ITEM-01/02/09 백엔드 완비 — 만기일 필드까지 다룸) 또는 **MOD-03 통장 폼**(SET-04 짝). 백엔드는 폭포 계열만 남음 — FLOW-01/03은 owner_only, FLOW-02는 그에 의존. 온보딩 **SCR-02**도 후보.
- 메모: ① **"경과" 해석 결정** — ERD는 "도래 시", 아키텍처 L94는 "end_date **경과** 항목"이라 후자(strict before)를 채택. 만기 당일은 ACTIVE 유지(그날 수령), 다음날 배치가 보관. ② **Clock 주입 테스트 패턴** — 통합 테스트에서 `@TestConfiguration` static nested + `@Bean @Primary Clock fixedClock()` + `@Import`로 기준일 고정. JWT도 같은 Clock을 쓰지만 이 테스트는 HTTP/JWT 미사용(리포지토리 직접). 별도 컨텍스트라 기존 테스트 영향 없음. ③ `LocalDate.now(clock)`는 인자 있어 checkstyle 통과(인자 없는 now()만 금지). ④ **@Scheduled은 04:00 cron이라 테스트 중 발화 안 함** — 로직은 서비스 직접 호출로 검증. ⑤ Flyway 새 버전 없음(`end_date`는 V1에 이미 존재, ddl-auto=validate 통과로 확인). ⑥ **함정**: Write 도구가 새 파일을 LF로 써서 spotless 빨간불 → `spotlessApply`로 CRLF 정정 후 verify(기존 sed-crlf 메모와 동류). ⑦ 커밋 함정: Bash 도구는 bash라 PowerShell `@'...'@` here-string이 안 먹혀 메시지에 `@` 혼입 → `git commit --amend`로 다중 `-m` 정정. ⑧ JAVA_HOME 여전히 jdk-22 우회 필요.

## 2026-06-13  —  [ITEM-09 완료] budget_items soft delete + 조회 제외
- 한 일: ITEM-01의 `ItemStatus`(ACTIVE/ARCHIVED/**DELETED**) 구조 위에 삭제만 얹음(owner_only 아님, 전체 위임). ① `BudgetItem.markDeleted()` 뮤테이터(status→DELETED, 행 잔존). ② `BudgetItemService.delete(userId, itemId)` — 기존 **`ownedActiveOrThrow` 단일 관문 재사용**이라 타인·부재·이미 삭제된 항목은 전부 NOT_FOUND(존재 비노출). ③ `BudgetItemController` `DELETE /api/v1/budget-items/{id}` → 204(Account.delete 패턴 미러링) + import/클래스 javadoc 갱신. ④ **조회 필터·count는 변경 없음** — ITEM-01의 ACTIVE-only 쿼리가 DELETED를 자동 제외(추가 코드 불필요). 과거 사이클 스냅샷(plan_lines)은 값 복사 보유라 삭제에 불변(규칙 4).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — **BudgetItemIntegrationTest 17건**(기존 12 + ITEM-09 5건 추가): 삭제 시 204·목록/단건 제외·**행 잔존 status=DELETED**(물리삭제 아님), 타인 항목 삭제 차단(NOT_FOUND·원본 ACTIVE 불변), 재삭제 NOT_FOUND, **삭제로 활성 100개 상한 자리 빔**(count가 DELETED 제외 확인), 토큰 없이 401. 단위·골든·ArchUnit·checkstyle 전부 유지(골든 무수정 통과). `npm run verify` 통과(프론트 미변경, 회귀 없음).
- passes 전환: **ITEM-09 → true**.
- 다음: **ITEM-02**(end_date 보유 항목 ARCHIVED 전환 배치 @Scheduled·KST·멱등 — 같은 패키지 연장, Clock 주입 멱등 테스트) 또는 짝 프론트 **MOD-01 항목 폼**/**MOD-03 통장 폼**. FLOW-02(overAllocated 응답 조립)도 후보지만 FLOW-01/03(owner)에 의존.
- 메모: ① **범위 한정** — ITEM-09는 ACTIVE 항목 삭제만. ARCHIVED 항목 삭제는 현재 경로에서 불가(ownedActiveOrThrow가 ACTIVE만 매칭) — 보관함/만기 흐름(ITEM-02/08) 도입 시 재검토. ② 과거 스냅샷 불변은 plan_lines가 값 복사 보유한다는 ERD 설계상 자동 보장 — plan_lines 엔티티는 아직 미구현이라 이번엔 "행 잔존 + status=DELETED" 수준으로 검증(스냅샷 테이블 생기면 교차 테스트 추가 여지). ③ 재삭제 NOT_FOUND는 Account.delete와 동일 동작(멱등이 아니라 활성만 삭제 가능). ④ **함정**: 테스트 메서드명에 가운뎃점(`·`, U+00B7)을 쓰면 Java 식별자 위반으로 spotless가 빨간불 — `과`/`_`로 치환. ⑤ Flyway 새 버전 없음(budget_items status는 V1에 이미 있음). ⑥ JAVA_HOME 여전히 jdk-22 우회 필요.

## 2026-06-13  —  [ITEM-01 완료] budget_items 생성·조회 + 통장 소유권 검증
- 한 일: `budgetitem` 패키지 신설 — `domain/Category`(순수 enum: SAVING/INVESTMENT/FIXED/INSURANCE/SUBSCRIPTION/EMERGENCY, **LIVING 제외** — 생활비는 폭포 나머지 계산값이라 항목 카테고리로 두지 않음, ERD 주석) + `infra/ItemStatus`(ACTIVE/ARCHIVED/DELETED 생명주기 마커) + `infra/BudgetItem`(엔티티, **ITEM-01 필드만 매핑**: user_id·account_id·category·name·amount(long 원)·start_date·memo·sort_order·status, 만기·이율·세금·input_meta·created_at/updated_at는 매핑 안 하고 DB 기본값/NULL로 보류) + `infra/BudgetItemRepository`(모든 조회에 user_id 동반: 상태별 목록 정렬·소유권+상태 fetch·활성 count·maxSortOrder) + `BudgetItemService`(생성·조회 유스케이스, 단일 소유권 관문 `ownedActiveOrThrow` + **대상 accountId가 호출자 활성 통장인지 검증** `requireOwnedActiveAccount` → 타인·삭제·없는 통장 전부 NOT_FOUND) + `BudgetItemController`(`GET /api/v1/budget-items`, `GET /{id}`, `POST` 201, bean validation: 금액 1~10억·이름 1~50·메모 ≤500). `common/ErrorCode`에 `ITEM_LIMIT_EXCEEDED(409)` 추가. `common/GlobalExceptionHandler`에 **`HttpMessageNotReadableException`→VALIDATION_FAILED** 핸들러 추가(미지원 enum=LIVING·잘못된 날짜 형식도 코드 기반 응답으로 통일). 활성 항목 100개 상한(구현규칙 6장). sortOrder는 생성 시 maxSortOrder+1로 끝자리 부여.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — **BudgetItemIntegrationTest 12건**(Testcontainers postgres:16-alpine + Flyway V1까지 실검증): 생성·sortOrder 0/1, 단건/목록 조회, **타인 항목 목록 비노출·단건 NOT_FOUND**, **타인/없는 통장 대상 생성 차단(NOT_FOUND, 항목 미생성 확인)**, LIVING 카테고리 거부·금액 0/10억초과·빈 이름 VALIDATION_FAILED, **활성 100개 상한 초과 409 ITEM_LIMIT_EXCEEDED**, 토큰 없이 401. 기존 단위·골든·ArchUnit·checkstyle·AuthIntegrationTest·AccountIntegrationTest 전부 유지(골든 무수정 통과). `npm run verify` 통과(프론트 미변경, 회귀 없음).
- passes 전환: **ITEM-01 → true**.
- 다음: **ITEM-09**(budget_items soft delete=status DELETED + 조회 필터 — 이번 status/ItemStatus 구조 위에 DELETE 엔드포인트만 얹으면 됨, 과거 스냅샷 불변 테스트) 또는 짝 프론트 **MOD-01 항목 폼**(ITEM-01 의존). ITEM-02(end_date ARCHIVED 배치)도 같은 패키지 연장.
- 메모: ① **범위 한정** — ITEM-01은 생성·조회만. 수정 적용 시점(ITEM-07)·soft delete(ITEM-09)·만기/이율/세금(ITEM-05)·일 단위 입력(ITEM-03)은 별도 요구사항이라 PATCH/DELETE 엔드포인트와 해당 컬럼 매핑을 의도적으로 보류 — 엔티티는 컬럼이 생기면 매핑만 추가하면 됨. ② **통장 소유권 2중 검증을 항목까지 확장** — 항목 자신의 소유권(BudgetItemRepository user_id 쿼리) + 대상 통장 소유권(AccountRepository.findByIdAndUserIdAndActiveTrue 재사용). 타인 통장에 항목 못 붙임. ③ `GlobalExceptionHandler`에 NotReadable 핸들러 추가는 전 엔드포인트 공통 이득 — Category enum을 타입 그대로 받되 미지원 값은 Jackson 파싱 실패→VALIDATION_FAILED로 깔끔히 떨어짐(별도 from() 파서 불필요). ④ created_at/updated_at는 미매핑 — DB `default now()`가 채움(Hibernate validate 모드는 미매핑 컬럼 무시, insert 시 DB 기본값 적용). ITEM-07 수정 도입 시 updatedAt 매핑·갱신 추가 필요. ⑤ Flyway 새 버전 없음(budget_items는 V1에 이미 있음). ⑥ JAVA_HOME 여전히 jdk-11 부재 → `$env:JAVA_HOME='C:/Program Files/Java/jdk-22'` 우회(영구 수정 권장).

## 2026-06-13  —  [SET-04 완료] 통장 CRUD + 소유권 검증 공통 처리 (Phase 1 진입)
- 한 일: `account` 패키지 신설 — `account/infra/Account`(엔티티, ERD accounts 1:1: name·purpose·bank_deep_link·sort_order·is_active, 계좌번호류 컬럼 없음/규칙 6) + `AccountRepository`(모든 조회에 user_id를 거는 파생 쿼리: 활성 목록 정렬·소유권+활성 동시검증 fetch·활성 count·maxSortOrder) + `AccountService`(CRUD 유스케이스, 소유권 단일 관문 `ownedOrThrow` → 미소유·삭제·부재 전부 NOT_FOUND로 존재 비노출) + `AccountController`(`GET|POST /api/v1/accounts`, `PATCH|DELETE /accounts/{id}`, principal=userId는 `@AuthenticationPrincipal Long`, POST 201·DELETE 204, DTO는 nested record + bean validation). `common/ErrorCode`에 `ACCOUNT_LIMIT_EXCEEDED(409)` 추가. soft delete=is_active false(규칙 5, 행 잔존 → 과거 스냅샷·plan_lines 참조 유지), 목록·조회는 활성만. 검증(구현규칙 6장): name 1~50자·purpose≤100·deepLink≤500·**활성 통장 20개 상한**. sortOrder는 생성 시 서버가 maxSortOrder+1로 끝자리 부여, PATCH는 값이 올 때만 재정렬.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — **AccountIntegrationTest 13건**(Testcontainers postgres:16-alpine + Flyway V1까지 실검증): 생성·sortOrder 0/1 부여, **소유권 위반 PATCH/DELETE 차단(타인 통장 → NOT_FOUND, 원본 불변)**, 타인 통장 목록 비노출, 수정 반영, **soft delete 후 목록 제외·행 잔존(is_active=false)**, 삭제분 재수정 거부, 20개 상한 초과 409, 삭제로 자리 나면 재생성 가능, 빈/51자 이름 VALIDATION_FAILED, 토큰 없이 401. 기존 단위·골든·ArchUnit·AuthIntegrationTest 전부 유지. `npm run verify` 통과(프론트 미변경, 회귀 없음).
- passes 전환: **SET-04 → true**.
- 다음: 짝 프론트 **MOD-03 통장 폼**(SET-04 의존) 또는 백엔드 **ITEM-01 budget_items CRUD**(account_id FK로 SET-04 위에 얹음). 온보딩 **SCR-02**도 후보(users PATCH/living_account_id는 별개 — SET-01 영역).
- 메모: ① 소유권 검증은 "리포지토리 쿼리에 user_id를 항상 포함 + 서비스 단일 관문"의 2중 강제 — 컨트롤러가 우회 불가. 후속 도메인(budget_items·envelopes)도 같은 패턴 권장(공통화 여지: 향후 base 소유권 헬퍼). ② `@AuthenticationPrincipal Long userId`는 JwtAuthenticationFilter가 principal에 Long userId를 넣어둔 덕에 바로 주입됨. ③ ACCOUNT_LIMIT_EXCEEDED는 409(CONFLICT)로 매핑 — CYCLE_ALREADY_EXISTS와 동급의 상태 충돌. ④ 통합 테스트에서 FK(accounts.user_id→users) 때문에 사용자 먼저 persist 후 jwtProvider.issue(id)로 실토큰 발급. ⑤ Flyway 새 버전 없음(accounts는 V1에 이미 있음). ⑥ 골든 케이스 무수정 통과. ⑦ JAVA_HOME 여전히 jdk-11 부재 경로 → `$env:JAVA_HOME='C:/Program Files/Java/jdk-22'` 우회로 gradle 실행(영구 수정 권장).

## 2026-06-13  —  [SCR-01 완료] 로그인 프론트 + 패키지 리네임(com.ngsoft→com.jinhyoung)
- 한 일: ① **SCR-01** — AUTH-01 백엔드(`POST /api/v1/auth/{provider}`→`{accessToken,isNewUser}`)와 짝인 프론트 로그인 흐름. `LoginView`(/login: 카카오·구글 버튼, 워드마크·태그라인·개인정보 안내, 동의 거부 시 `?error=CODE`로 복귀해 코드→i18n 표시), `AuthCallbackView`(/login/callback/:provider: redirect로 받은 code를 서버와 교환→세션 저장→홈, 실패·미지원·code없음은 각 에러코드로 로그인 복귀), `api/client`(axios 인스턴스 baseURL `/api/v1` + 요청 인터셉터 Bearer 부착 + 응답 인터셉터 401→logout·로그인 이동/그 외 `ApiError(code)` 정규화), `api/oauth`(공급자 authorize URL 빌더, client_id는 `VITE_*`/비밀키는 서버 전용), `stores/auth`(Pinia: token·isNewUser·isAuthenticated, JWT만 localStorage 영속), router 가드(비로그인→/login 차단, 로그인 상태의 /login→홈, 로그인·콜백은 `chrome:false`로 BottomNav 숨김), i18n ko/en(`login.*`·`errors.{code}`). 테스트 20건 신규. ② **패키지 리네임** — 초기 골격 때 회사 이메일 도메인(ngsofti.com)에서 따온 `com.ngsoft.salary`를 개인 프로젝트답게 `com.jinhyoung.salary`로 정정(디렉터리 git mv 이력 보존, package·import·ArchUnit 기준 문자열·build.gradle group 일괄 치환, 40파일).
- 초록불: `npm run verify` 통과(lint 0/0·type-check·**vitest 23/23**·build) + `./gradlew clean verify` BUILD SUCCESSFUL(단위·골든·ArchUnit + **Testcontainers 통합 테스트** 패키지 이동 후 클린 재빌드 실검증). 브라우저 검증: 미인증 `/`→`/login` 리다이렉트, 로그인 페이지 렌더·콘솔 에러 0, `?error=OAUTH_EXCHANGE_FAILED`→i18n 문구 매핑 확인.
- passes 전환: **SCR-01 → true**.
- 다음: **SCR-02 온보딩**(실수령액·월급일·조정 규칙 입력 — 첫 가입 흐름, 로그인 직후 분기) 또는 백엔드 **SET-04**(accounts CRUD, Phase 1 진입).
- 메모: ① **axios 의존성 추가**(아키텍처.md가 axios 인스턴스+JWT 인터셉터를 정본으로 규정). `frontend/.env.example`에 `VITE_KAKAO_CLIENT_ID`/`VITE_GOOGLE_CLIENT_ID` 문서화(실값은 `.env.local`, gitignore). ② 콜백 성공 시 현재 항상 홈으로 — 첫 가입(`isNewUser`) 온보딩 분기는 SCR-02에서 라우트 연결 예정(신호는 `auth.isNewUser`에 보관). ③ 실제 OAuth 공급자 왕복은 client ID 설정 필요 — 라우팅·교환 흐름은 통합 테스트(mock 교환)+백엔드 AUTH-01 실검증으로 커버. ④ **테스트 함정**: 싱글톤 라우터+다중 pinia 테스트에서 `mount`의 pinia 플러그인이 전역 active pinia를 갱신 안 함 → 단언 시 `useAuthStore(pinia)`로 인스턴스 명시. 동일 경로 push는 내비게이션 무시(가드 테스트는 다른 경로에서 출발). fire-and-forget `router.replace`는 최종 currentRoute 대신 spy로 호출 의도 검증. ⑤ **리네임 함정**: git-bash `sed -i`가 CRLF→LF로 바꿔 spotless 빨간불 → `spotlessApply`로 정정(round-trip 상쇄, 줄바꿈 diff 0). ⑥ progress.md 과거 로그의 `com.ngsoft` 프로즈는 히스토리라 보존. ⑦ Flyway 새 버전 없음.

## 2026-06-13  —  [AUTH-01 완료 + AUTH-03] 로그인 흐름 전체 + 스테이트리스 보안
- 한 일: 기존 코어(어댑터 3종·JwtProvider) 위에 로그인 흐름을 끝까지 배선. ① `OAuthClient` 포트 + `RestClientOAuthClient`(code→토큰 교환→userinfo→어댑터 정규화, 실패 시 OAUTH_EXCHANGE_FAILED). ② `AuthService`: `(provider, provider_id)`로 upsert + JWT 발급, 신규는 isNewUser=true. ③ `AuthController` `POST /api/v1/auth/{provider}` → `{accessToken, isNewUser}`. ④ `User` 엔티티(**user.infra**)·`UserRepository`(findByProviderAndProviderId). ⑤ `SecurityConfig` 스테이트리스(세션 없음, /api/v1/auth/** 공개, 그 외 JWT 필수) + `JwtAuthenticationFilter`(principal=userId) + `JwtAuthenticationEntryPoint`(401 {code:UNAUTHORIZED}). ⑥ `common`: `ErrorCode`/`ApiException`/`ApiErrorResponse`/`GlobalExceptionHandler`({code,params}), `TimeConfig`(KST `Clock` 빈). ⑦ `AuthProperties`(app.jwt·app.oauth), application.yml에 dev 기본값(시크릿은 env 주입). ⑧ build.gradle: spring-security-test + Testcontainers(postgresql·junit-jupiter·spring-boot-testcontainers).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL — **AuthIntegrationTest 9건**(신규/재로그인 멱등/AUTH-03 공급자분리/닉네임 폴백/비활성 naver/미지원 provider/code 검증실패/보안 401·통과) + 기존 단위·골든·ArchUnit 전부. 통합 테스트가 Testcontainers PostgreSQL(postgres:16-alpine)을 띄워 **Flyway V1 + ddl-auto=validate까지 실검증**. `npm run verify` 통과(프론트 미변경).
- passes 전환: **AUTH-01 → true, AUTH-03 → true**.
- 다음: **SCR-01**(로그인 페이지 + JWT 저장·axios 인터셉터 + 가드 라우팅, 프론트) — 이번 백엔드와 짝. 또는 Phase 1 SET-04(accounts CRUD)/ITEM-01.
- 메모: ① **신규 사용자 플레이스홀더 결정**: ERD users의 base_income/payday/payday_adjustment가 NOT NULL인데 OAuth 가입은 온보딩 전이라 값이 없다 → `0 / 1 / NONE`으로 행 생성하고 isNewUser=true로 온보딩 유도(온보딩에서 덮어씀). User.java에 상수로 명시. ② 닉네임 NOT NULL인데 동의 거부 시 null → 이메일 로컬파트 → `provider_id` 순 폴백(데이터 기본값, UI 문구 아님). ③ 통합 테스트는 Testcontainers 채택(CI ci.yml에 PG 서비스 없음 — ubuntu 러너 docker로 컨테이너 기동). ④ JWT dev 시크릿은 32바이트↑ placeholder를 yml에 둠(실값은 JWT_SECRET 등 env). ⑤ JAVA_HOME이 여전히 jdk-11(부재) 가리킴 → gradle 호출 시 `$env:JAVA_HOME='...jdk-22'` 임시 지정으로 우회(영구 수정 권장). ⑥ Flyway는 새 버전 추가 없음 — V1 그대로 사용.

## 2026-06-13  —  [ENV-setup 완료] Docker/WSL2 셋업 + Flyway V1 실검증
- 한 일: 로컬에 Docker Desktop 설치(winget) + WSL2 설치(wsl --install) + 재부팅으로 엔진 기동. `docker compose up -d`로 PostgreSQL 16 컨테이너(salary-db) 기동, `./gradlew bootRun`으로 Flyway V1 실적용 검증.
- 초록불: Flyway "Successfully validated 1 migration" → "Migrating schema public to version 1 - init" → "Successfully applied". DB에서 \dt로 ERD 11테이블 전부 확인(+flyway_schema_history), flyway_schema_history version 1 success=t. ENV-setup의 마지막 미검증 항목(백엔드 DB 기동) 해소.
- passes 전환: **ENV-setup → true**.
- 다음: AUTH-01 백엔드 마무리 — User 엔티티·UserRepository, OAuth 코드 교환 클라이언트(공급자 secret), AuthService(upsert+JWT), POST /api/v1/auth/{provider}, SecurityConfig(스테이트리스+JWT 필터), 로그인 흐름 통합 테스트. AUTH-03(provider,provider_id 분리)도 함께. (코어 어댑터·JwtProvider는 이미 완료.)
- 메모: ① bootRun이 "Port 8080 already in use"로 종료했으나 이는 마이그레이션 이후 웹서버 바인딩 단계 — V1 적용은 그 전에 성공. 8080 점유 프로세스 정리 필요(통합 테스트는 random port라 무관). ② docker는 PATH에 `C:\Program Files\Docker\Docker\resources\bin` 추가 또는 새 셸 필요. ③ Windows 11 Home은 Hyper-V 없어 Docker가 WSL2 필수였음. ④ salary-db 컨테이너는 띄워둔 상태 — AUTH-01 작업에 사용.

## 2026-06-13  —  [AUTH-01 코어] OAuth 정규화 어댑터 + JwtProvider (docker-free 부분)
- 한 일: AUTH-01 중 DB 없이 완전 초록불 가능한 코어. `auth` 패키지: `OAuthProvider`(KAKAO/GOOGLE enabled, NAVER 비활성 + from() 파싱), `OAuthUserInfo`(정규화 record), `OAuthAttributesMapper` + 카카오/구글/네이버 어댑터 3종(공급자별 JSON Map → 정규화, 순수). `JwtProvider`(HS256, subject=userId, 발급·검증, **Clock 주입**으로 만료 결정론 — 규칙 3). build.gradle에 jjwt 0.12.6(api+impl+jackson) 추가. 단위 테스트: 어댑터 6건(중첩 구조·동의거부 null·필수 식별자 거부·provider 파싱·네이버 비활성), JwtProvider 3건(복원·만료 거부·위조 거부).
- 초록불: `./gradlew verify` BUILD SUCCESSFUL. 신규 9건 포함 전체 통과. (프론트 미변경.)
- passes 전환: 없음 — AUTH-01은 **코어만**. 남은 부분: 코드 교환 OAuth 클라이언트(공급자 secret 필요), 로그인 성공 → users upsert(User 엔티티·repo·DB), `POST /api/v1/auth/{provider}` 컨트롤러, Security 스테이트리스+JWT 필터, **로그인 흐름 통합 테스트(DB 필요 → docker)**. AUTH-03(unique(provider,provider_id) 분리)은 upsert 단계에서 함께.
- 다음: (docker 확보 후) ENV-setup V1 bootRun 검증 → AUTH-01 나머지(엔티티·upsert·컨트롤러·security·통합테스트). 통합 테스트 도입 시 V1 마이그레이션도 함께 실검증됨.
- 메모: ① auth 코어는 com.ngsoft.salary.auth(=..domain.. 아님)라 jjwt 의존 무방. 순수 변환이라 단위 테스트만으로 충분. ② JWT는 java.util.Date를 쓰지만 Clock.instant()로만 생성 — now() 직접 호출 없음(checkstyle 통과). ③ HS256 키 32바이트↑ 필요 — 테스트 시크릿도 그 길이. ④ API 형태: POST /api/v1/auth/{provider} {code} → {accessToken, isNewUser} (API명세 2장).

## 2026-06-13  —  [ENV-setup 대부분] Flyway V1 + 프론트 기반(i18n·토큰·컴포넌트·대시보드)
- 한 일: ① 백엔드 `db/migration/V1__init.sql` — ERD v1.2의 11테이블(users/accounts/budget_items/envelopes/envelope_transactions/cycles/plan_lines/check_ins/suggestions/notification_logs/holidays). 금액 bigint, 날짜 date, 시각 timestamptz, enum류 varchar, jsonb(input_meta·payload). 순환 FK(users.living_account_id↔accounts)는 ALTER로 해소, 멱등 unique 제약 포함. ② 프론트: vue-i18n(legacy:false·globalInjection) + locales/ko·en.json, styles/tokens.css(화면설계.html 토큰 1:1·Pretendard), components/base 5종(Card·MoneyText·ProgressBar·BottomSheet·BottomNav), App 셸(모바일 퍼스트 430px)+빈 대시보드 HomeView, 라우터 정리, create-vue 스캐폴드 잔재 제거. base 컴포넌트 한정 multi-word 규칙 off. MoneyText 단위 테스트 3건.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL, `npm run verify` 통과(lint 0/0·type-check·vitest 3/3·build). 프론트 dev 서버 브라우저 기동 확인 — 빈 대시보드 렌더, 콘솔 에러 0, $t 템플릿 주입 정상.
- passes 전환: 없음 — ENV-setup은 **대부분 완료**, flag는 false 유지. 미검증 1건: 백엔드가 PG에 Flyway V1을 실제 적용하며 뜨는지(`docker compose up -d && ./gradlew bootRun`). 이 환경엔 docker/psql이 없어 마이그레이션 런타임 적용을 못 돌림. CI(ci.yml)는 이미 양쪽 verify를 그대로 수행 — ENV-setup의 CI 조건 충족.
- 다음: (소유자) docker 기동 후 bootRun으로 V1 적용 확인 → 문제없으면 ENV-setup passes:true. 그 후 AUTH-01(OAuth2+JWT) 또는 SET-04(accounts CRUD). HARNESS-golden 잔여(waterfall/payday fixture)도 여전히 소유자 단계.
- 메모: ① V1은 docker 부재로 런타임 미적용 — 첫 bootRun이 실질 검증(ddl-auto=validate라 엔티티 없는 현재는 통과). SQL 오타 리스크 있으니 bootRun 로그 확인할 것. ② launch.json(.claude/) 추가 — preview용 frontend dev 서버(port 5173). ③ vue/multi-word-component-names가 flat/essential에 포함돼 'Card'가 걸림 → base 디렉터리 한정 off. ④ Pretendard는 tokens.css에서 CDN @import(추후 self-host 검토).

## 2026-06-13  —  [HARNESS-golden 부분] 만기 골든 테스트 + MaturityCalculator
- 한 일: 잠긴 fixture(maturity-cases.json)를 실제로 강제하는 골든 테스트를 만들었다. ① `budgetitem/domain`에 순수 클래스 추가 — `MaturityCalculator`(단리: 이자=월납입×연이율×n(n+1)/2÷1200 → 원미만 HALF_UP, 세금=이자×세율 → 원미만 HALF_UP, 만기=원금+이자−세금), `MaturityInput`(record, 검증 포함), `MaturityResult`(원금·이자·세금·total 분해), `TaxType`(NORMAL_15_4=0.154, TAX_FREE). 전부 BigDecimal/long(double 금지). ② `golden/MaturityGoldenTest` — fixture를 @TestFactory로 읽어 두 실데이터 케이스(3,731,976 / 2,476,986)를 계산기가 재현하는지 검증. Jackson 의존이라 ..domain.. 밖 golden 패키지에 둠(ArchUnit이 테스트도 스캔). ③ `MaturityCalculatorTest` 단위 테스트 — 비과세·세금 HALF_UP(808.5→809)·입력 검증 경계.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL. MaturityGoldenTest 2/2(만기 케이스), MaturityCalculatorTest 3/3, RuleEnforcementTest 3/3, 골든 무결성 통과. `npm run verify`는 직전 세션과 동일(미변경).
- passes 전환: 없음 — HARNESS-golden은 **부분 완료**. 만기 골든은 됐으나 waterfall-cases.json·payday-cases.json·시드 스크립트가 남음. 그 부분은 owner_only(WaterfallCalculator·PaydayResolver는 소유자 작성) + 기대값이 owner 실데이터 + 새 fixture는 goldenLock(소유자 전용) + 시드는 스키마(ENV-setup의 Flyway V1) 필요 → 협의·소유자 단계.
- 다음: HARNESS-golden 잔여(소유자와): waterfall/payday fixture 작성 → goldenLock. 또는 ENV-setup(Flyway V1__init.sql 11테이블)을 먼저 해 시드·스키마 기반 마련. (트래커 순서상 HARNESS-claude도 남음.)
- 메모: ① 만기 공식은 fixture로 교차검증 — 두 케이스 정확 일치. ② Jackson 쓰는 골든/통합 테스트는 ..domain..에 두면 domainIsFrameworkFree에 걸린다 → golden 등 별도 패키지. ③ 새 골든 fixture를 추가하면 GoldenFixtureIntegrityTest가 goldenLock 전까지 빨간불 — 그래서 이번엔 새 fixture 없이 기존 fixture만 소비. ④ MaturityResult 분해값은 ITEM-05 표시용 선반영.

## 2026-06-13  —  [HARNESS-archunit] 규칙 음성 테스트 + 윈도우 golden 무결성 복구
- 한 일: ① main에서 `./gradlew verify`가 빨간불이었음 — GoldenFixtureIntegrityTest가 sha256 불일치. 원인은 `core.autocrlf=true`+`.gitattributes` 부재로 golden fixture가 CRLF로 체크아웃돼 바이트 해시가 깨진 것(내용은 정당). repo 루트에 `.gitattributes` 추가(golden 경로 `eol=lf` 고정)하고 작업 트리 재정규화로 복구 — fixture 수정·재lock 없이. ② HARNESS-archunit 마무리: 기존 ArchitectureTest(규칙2 double/float·규칙9 domain 순수)와 checkstyle(규칙3 now() 금지)은 있었으나 "위반 시 실패" 증명이 없었음. `RuleEnforcementTest` 추가 — com.ngsoft.salary 밖 `archfixtures` 위반 픽스처(double 필드 / domain 패키지의 스프링 의존)를 명시 임포트해 두 ArchRule이 실제로 AssertionError를 던지는지, checkstyle 정규식을 config에서 읽어 인자 없는 now()는 잡고 now(clock)는 통과하는지 검증.
- 초록불: `./gradlew verify` BUILD SUCCESSFUL (RuleEnforcementTest 3/3, 골든 무결성 통과). `npm run verify` 통과(npm install 후). JAVA_HOME이 삭제된 jdk-11을 가리켜 JDK 22로 우회 실행.
- passes 전환: HARNESS-archunit → true.
- 다음: HARNESS-golden(골든 fixture 폭포 기대값·PaydayResolver 케이스 + 시드 스크립트) → HARNESS-claude → ENV-setup. (owner 메모는 AUTH-01을 가리켰으나 트래커 순서상 harness 항목이 먼저 — 협의.)
- 메모: ① 음성 테스트 픽스처는 com.ngsoft.salary 밖 `archfixtures`/`archfixtures.domain`에 둬야 전역 @AnalyzeClasses 스캔에 안 걸린다. ② 테스트 소스에 `LocalDate.now()` 리터럴을 두면 checkstyle이 잡으므로(=규칙 작동 방증) 문자열을 런타임 합성. ③ checkstyle.xml 파싱 시 외부 DTD(checkstyle.org) 로드 비활성화 — 오프라인 동작. ④ 환경 JAVA_HOME 고장(jdk-11 경로 없음) — 영구 수정 권장.

## 2026-06-11  —  [HARNESS-verify] Phase 0 골격 생성 + verify 게이트 가동
- 한 일: zip 해제(D:\01_PROJECT\07_SALARY_APP) + git init. Spring Initializr(Boot 3.5.15·Java 21·Gradle 8.14.5) 골격을 backend/에 병합, `com.example.salary`→`com.ngsoft.salary` 전부 치환. settings.gradle에 foojay-resolver 추가(로컬 JDK 17뿐이라 toolchain이 JDK 21 자동 다운로드). application.yml 작성(flyway validate-on-migrate, datasource는 DB_* 환경변수+로컬 기본값). create-vue로 frontend/ 생성 후 verify 스크립트 병합. golden/maturity-cases.json input을 실데이터로 채움 — 단리→15.4% 공식으로 기대값 3,731,976 / 2,476,986 일치 교차검증. 공개 레포 대비 비식별화: 실수령액·적금 상품명·정밀 지출액을 합산이 맞는 가상 수치 세트로 교체(목업·API명세·개발계획·fixture). docker-compose.yml(PostgreSQL 16) 추가. CLAUDE.md 실행 명령 사전·README 실행 방법 채움.
- 초록불: `npm run verify` 완전 통과(oxlint·eslint 0건, vue-tsc, vitest 1건, build). `./gradlew verify`도 완전 통과 — 소유자가 goldenLock 직접 실행(같은 날)해 golden.sha256 생성 후 BUILD SUCCESSFUL 확인. 골든 케이스: 적금 만기 3,731,976 / 2,476,986.
- passes 전환: HARNESS-verify → true.
- 다음: 첫 커밋·push(origin = github.com/JINHYOUNG00/Side, main) → AUTH-01.
- 메모: ① Initializr 기본 contextLoads 테스트는 DB 필요(JPA datasource)라 제외 — 통합 테스트는 AUTH-01에서 도입. ② 2026 create-vue는 oxlint+eslint 조합에 tsconfig가 project references라 스니펫의 `vue-tsc --noEmit` 대신 `vue-tsc --build` 사용(verify 의미는 동일: 검사 전용). ③ ArchitectureTest domain 규칙에 `allowEmptyShould(true)` 추가 — ArchUnit 0.23+는 빈 매칭 집합이면 실패하므로 domain 패키지 생기기 전에도 초록 유지. ④ fixture 기대값은 구현규칙 1장 공식 기준 — 은행 실수령과 수원 단위 차이 가능(ITEM-05 고지 대상). ⑤ spotlessApply가 키트 테스트 2개를 palantir 스타일로 재포맷함(정상).

## 2026-06-11  —  [통합] 최종 패키지 조립 (리뷰 반영)
- 한 일: docs/아키텍처.md 추가(엔티티는 infra, domain은 완전 순수 — ArchitectureTest의 엄격 규칙과 정합). settings.json deny에 goldenLock 4종 추가(에이전트 셀프 승인 차단 — goldenLock은 소유자 전용). 백로그 md를 docs/_archive/로 이동, 추적기는 feature_list.json로 일원화. README 문서 목록 갱신.
- 초록불: 아직 없음 — 빈 프로젝트.
- 다음: START-HERE 1~8 순서대로 Phase 0 (HARNESS-verify부터).
- 메모: JPA 엔티티는 각 기능의 infra 패키지에 둘 것(아키텍처.md v1.1 개정). goldenLock은 사람이 직접 실행.

## (초기) — 하네스 스캐폴딩 제공됨
- 한 일: feature_list.json, progress.md, .claude/(settings.json·commands·hooks) 생성. CLAUDE.md는 기존 것 유지.
- 초록불: 아직 없음 — 빈 프로젝트, 모든 항목 failing.
- 다음: Phase 0 HARNESS-verify → ENV-setup. CLAUDE.md 실행 명령 사전을 실제 명령으로 채우고, .claude/hooks 경로(골든 fixture·마이그레이션 위치) 확인.
- 메모: 1인 프로젝트라 모든 하네스 파일을 그냥 커밋한다(.local 불필요).
